{
  "name": "monster",
  "version": "0.17.0",
  "private": true,
  "scripts": {
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "oxfmt": "^0.41.0",
    "oxlint": "^1.56.0",
    "playwright": "^1.58.2"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "oxlint --fix",
      "oxfmt --write"
    ]
  },
  "packageManager": "pnpm@10.30.3",
  "pnpm": {
    "overrides": {
      "ioredis": "5.9.3"
    },
    "onlyBuiltDependencies": [
      "cpu-features",
      "ssh2"
    ]
  }
}
