{
  "name": "@monster/analytics",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {},
  "scripts": {
    "build": "esbuild src/tracker.ts --bundle --minify --format=iife --platform=browser --target=es2017 --outfile=dist/tracker.min.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  }
}
