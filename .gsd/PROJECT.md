# BuilderMonster

## What This Is

Single admin panel to manage the full lifecycle of a website portfolio: AI-assisted generation, deployment, SEO optimization, and continuous maintenance. The AI is the core engine — generates content, researches niches, scores SEO, recommends improvements, and keeps sites updated. Business model: self-operated portfolio (not SaaS). Evolution of the validated `tsa-monster` project (Laravel + FilamentPHP + Astro.js, TSA sites in production).

## Core Value

A working end-to-end pipeline: idea → niche research → site generated → deployed → ranking → earning. Everything else supports this loop.

## Current State

M001/S01 complete: monorepo scaffold with pnpm workspaces, all package directories created, worktree script ready.
M001/S02 complete: full Phase 1 schema applied to Supabase Cloud (21 tables, 7 migrations), TypeScript types generated (1218 lines) and committed to `packages/db/src/types/supabase.ts`. All 4 Supabase env vars in `.env`.
M001/S03 complete: `packages/db` exports typed Supabase client factories (createBrowserClient, createServiceClient) with Database generic + full type re-exports; `packages/shared` exports domain types and constants (AMAZON_MARKETS, SUPPORTED_LANGUAGES, SITE_STATUS_FLOW, REBUILD_TRIGGERS); both packages build cleanly to ESM + .d.ts; `apps/admin` resolves both via workspace:* with tsc --noEmit exit 0.
M001/S04 complete: Next.js 15 admin panel (`apps/admin`) builds cleanly and runs on port 3004. Supabase Auth wired end-to-end — middleware protects all 7 routes (getAll/setAll cookie interface, getUser()), login/logout server actions work, protected (dashboard) layout with dark sidebar nav renders all 7 sections (stubs). `pnpm build` and `tsc --noEmit` both exit 0.
M001/S05 complete: pm2 runs monster-admin on port 3004 (online, 0 restarts, HTTP 200). `ecosystem.config.js` uses correct script path (`node_modules/next/dist/bin/next`). `scripts/deploy.sh` handles full cycle: pull → install --frozen-lockfile → build → pm2 reload || start → pm2 save. `logs/` dir tracked with `.gitkeep`. Process list saved to `~/.pm2/dump.pm2`. `M001-SUMMARY.md` documents worktree, deploy, and pm2 protocols. **M001 Foundation milestone complete.** One manual step remaining: `pm2 startup systemd` (sudo required, documented).
M002/S01 complete: Sites CRUD fully operational — create/view/edit round-trips to Supabase via service role client. `SiteCustomizationSchema` in `packages/shared` (M003-ready). Server action pattern established (useActionState + 'use client' wrapper, return errors vs throw). Nav active state via NavItem client component. All 13 routes build cleanly. `tsc --noEmit` exits 0. Browser-based visual UAT pending (Playwright not executable on host).
M002/S02 complete: Dashboard KPIs live — four real Supabase count queries (total sites, live, draft, open alerts) fire in parallel via Promise.all, rendered as KPI cards. Replaces "Coming soon" stub. `tsc --noEmit` exits 0, build clean, pm2 reload clean, 307 on /dashboard confirms route healthy.
M002/S03 complete: Settings page fully operational — server component reads API keys from Supabase (masked last-4 display), saveSettings server action upserts with onConflict:'key' and { value: rawKey } JSON wrapper, client form uses useActionState with success/error banners. No raw key value in HTML source. Constants-in-sibling pattern established for 'use server' files (D034). `tsc --noEmit` exits 0, build clean, pm2 reload → 307 on /settings.
M002/S04 complete: Finances shell fully operational — addCost server action (Zod validation + Supabase insert + revalidatePath), CostForm client component (useActionState, native selects, success banner, per-field errors), /finances/page.tsx (parallel fetch of costs/categories/sites, cost list table with empty state, revenue placeholder card). All slice verification checks pass. **M002 Admin Panel MVP milestone complete.** M002-SUMMARY.md written.
M003/S01 complete: Astro 6 + Tailwind v4 generator in `apps/generator` with three templates (Classic, Modern, Minimal) across all page types (homepage, category, product, 4 legal). Data injection via `src/data/<slug>/site.json`. BullMQ worker in `packages/agents` reads site from Supabase, writes fixture site.json, calls Astro `build()` programmatically, tracks progress in `ai_jobs`. Admin panel "Generate Site" button + 5-second polling JobStatus component. Full click-to-dist flow verified: 11 pages built, affiliate links correct, no hotlinked images. `tsc --noEmit` and `pnpm -r build` both exit 0.
M003/S02 complete: Real DataForSEO Merchant API pipeline replacing fixture assembler. `DataForSEOClient` (task_post → poll → task_get, exponential backoff, credentials from Supabase settings). Sharp WebP image pipeline (`downloadAndConvertImage`, `processImages` with p-limit 5). `GenerateSiteJob` three-phase pipeline: fetch_products → process_images → build with `ai_jobs.payload` progress tracking. `tsa_categories`, `tsa_products`, `category_products` upserts with idempotent onConflict semantics. `SiteData` assembled from DB rows post-upsert. No Amazon CDN URLs in built HTML (enforced structurally). Known: Amazon CDN blocks Node.js User-Agent — images degrade gracefully to `[]`. `tsc --noEmit` and build both exit 0. Live end-to-end pending DataForSEO credentials in admin Settings.
M003/S03 complete: `ContentGenerator` class in `packages/agents` using `@anthropic-ai/sdk` + Zod v4 structured output. `CategoryContentSchema` and `ProductContentSchema` generate SEO texts, product descriptions, pros/cons, user opinion summaries, meta descriptions — all in the site's configured language. Idempotent (skips if `focus_keyword` non-null in DB), throttle-aware (1.5s sleep + maxRetries: 5). `generate_content` phase wired into `GenerateSiteJob` between `process_images` and `build`; `ai_jobs.payload` updated per item. `lockDuration: 300000` on BullMQ Worker. `SiteData` interfaces extended with `focus_keyword`/`meta_description`/`user_opinions_summary`; `BaseLayout.astro` emits `<meta name="description">` when populated. All three template layouts forward `metaDescription`. `tsc --noEmit`, `pnpm --filter @monster/agents build`, and `astro check` all exit 0. Key deviation: `zodOutputFormat` lives at `@anthropic-ai/sdk/helpers/zod` (D060). Category meta_description reuses `tsa_categories.description` column (D057); product meta_description in-memory only (D058).
M003/S04 complete: `@monster/seo-scorer` package with `scorePage(html, focusKeyword, pageType): SeoScore` — 8 weighted categories (content_quality 30%, meta_elements 20%, structure 15%, links 12%, media 8%, schema 8%, technical 5%, social 2%), legal page keyword exemption, Flesch null-safety, grade A–F. 8 unit tests all pass. Unique constraint migration `seo_scores_site_page_unique(site_id, page_path)` enables idempotent upserts. `score_pages` phase wired into `GenerateSiteJob` after Astro build (non-fatal per-page errors, batch upsert, ai_jobs.payload progress). SEO Scores card added to admin panel site detail page (server-side query, 12-column table, grade badges, empty state). All builds and typechecks pass. **M003 TSA Site Generator milestone complete.** R004 (ContentGenerator), R005 (SEO Scorer), R015 (3 templates) validated. Operational end-to-end run (with real DataForSEO + Anthropic credentials) is the first task for M004 context — required to confirm ≥80% pages score ≥70 and to fix the Amazon CDN User-Agent issue before deployment.
M004/S01 complete: `packages/deployment` built with `RsyncService` (child_process.spawn + rsync --delete over SSH) and `CaddyService` (node-ssh + SSH agent + sudo tee + systemctl reload caddy). Amazon CDN User-Agent fix applied to `downloadAndConvertImage()` (D069). Settings UI extended with VPS2 Deployment card (vps2_host, vps2_user, vps2_sites_root). pnpm onlyBuiltDependencies configured for ssh2/cpu-features native crypto. All four builds exit 0. Both services export cleanly; RsyncService diagnostic surface verified (error includes exit code, slug, host, full stderr). S02 (Cloudflare automation + deploy pipeline) is unblocked.
M004/S02 complete: Full deploy pipeline wired end-to-end. `packages/domains` bootstrapped with `CloudflareClient` (ensureZone idempotent, ensureARecord idempotent, pollSslStatus). DB migration adds `cf_nameservers text[]` to `domains` table. `runDeployPhase()` shared helper orchestrates rsync → Caddy → CF zone+A record → state transitions → SslPollerJob enqueue. `DeploySiteJob` on queue `'deploy'` (standalone redeploys). `SslPollerJob` on queue `'ssl-poller'` (delayed re-enqueue, 30-attempt limit, transitions `dns_pending → ssl_pending → live`). Deploy phase wired into `GenerateSiteJob` after `score_pages`. Admin panel: "Deploy" button (disabled if no domain), `DeployStatus` polling component, Deployment card (pipeline status + latest deployment + CF nameservers). Settings: `cloudflare_api_token` + `vps2_ip` inputs in Cloudflare card. All four builds exit 0. Pending: apply migration to remote Supabase; enter CF token + VPS2 IP in Settings; live deploy + NS propagation + curl CF-RAY verification (human UAT).
M004/S03 complete: `SpaceshipClient` in `packages/domains` with four methods (checkAvailability, registerDomain, pollOperation, updateNameservers). Settings keys `spaceship_api_secret` + `spaceship_contact_id` added (3-file settings pattern — 11 keys total). `checkDomainAvailability` + `registerDomain` server actions in site detail `actions.ts`. `DomainManagement` client component: availability check form + R031-gated registration form (hidden until available, red-border warning, explicit form submit only). Domain Management card added to site detail page. `@monster/domains` added to admin package.json. All builds exit 0. **M004 Deployment + Cloudflare milestone complete.** Pending: live Spaceship credentials in Settings + live end-to-end registration UAT (requires real domain, real credentials, deployed site with cf_nameservers populated).
M005/S01 complete: `packages/analytics` esbuild pipeline produces `dist/tracker.min.js` at 1343 bytes (≤2KB). Tracker: SubtleCrypto SHA-256 visitor hash (Math.random fallback for non-HTTPS), fetch+keepalive POST transport (sendBeacon rejected — cannot set PostgREST auth headers), `pageview` on load, `click_affiliate` on `[data-affiliate]` links, 5s flush interval + visibilitychange flush. `SiteInfo` extended with `id/supabase_url/supabase_anon_key`; `GenerateSiteJob` writes credentials to `site.json`; `BaseLayout.astro` reads `tracker.min.js` via `readFileSync` at Astro build time and substitutes 3 placeholders before inline injection. All three template layouts thread analytics props. `data-affiliate={product.asin}` on all affiliate `<a>` tags. astro check 0 errors, all builds exit 0. Live runtime proof (rows in `analytics_events`) deferred to human UAT.
M005/S02 complete: Admin panel `/analytics` page replaced "Coming soon" stub with a full server-component dashboard. `analytics/lib.ts` provides `getDateRange`, `computeMetrics` (pure in-memory JS reducer), and `fetchAnalyticsData` (parallel Supabase fetch via Promise.all, conditional site filter, throws on DB error). `page.tsx` renders KPI cards (total pageviews, unique visitors approximate, affiliate clicks), per-site metrics table, top pages table, Daily Aggregates section (graceful empty state until S03 runs), and Country Breakdown Phase 1 placeholder. `AnalyticsFilters.tsx` is 'use client' with native `<form method="GET">` — shadcn Select incompatible with form serialization (D086). Both selects auto-submit on change; URL is bookmarkable. tsc --noEmit exit 0, pnpm build exit 0, pm2 monster-admin online.
M005/S03 complete: `AnalyticsAggregationJob` in `packages/agents` aggregates `analytics_events` → `analytics_daily` via in-memory Map grouping by `${siteId}::${pagePath}`, upsert with conflict on `(site_id, date, page_path)`. BullMQ repeat job registered via `upsertJobScheduler` (stable ID `'analytics-daily-aggregation'`, cron `0 2 * * *` UTC) on worker startup — idempotent on restart. `monster-worker` pm2 entry in `ecosystem.config.js`. `enqueueAnalyticsAggregation` server action + `AggregationTrigger` client component with pending state and inline status wired into `/analytics` page header. Both builds exit 0. Runtime verification blocked by pre-existing `node-ssh` ERR_MODULE_NOT_FOUND (M004 issue). **M005 Analytics milestone complete. M005-SUMMARY.md written.** First priority for M006: fix node-ssh ERR_MODULE_NOT_FOUND so monster-worker can start → human UAT (visit live site → events in analytics_events → run aggregation → analytics_daily row → dashboard confirms counts → R009 validated).
M006/S01 complete: `node-ssh` ERR_MODULE_NOT_FOUND fixed (direct dep in agents package). `monster-worker` starts cleanly. `ProductRefreshJob` scaffolded with `fetch_products` phase — fetches DataForSEO products, writes `last_refreshed_at`/`next_refresh_at` to DB. `productRefreshQueue()` singleton + `enqueueProductRefresh(siteId)` server action. Site detail page shows "Last refreshed: X hours ago" with "Refresh Now" button. Cron scheduler registers per-site refresh jobs on startup.
M006/S02 complete: Diff engine (`diffProducts()` pure function) categorizes price/availability/image changes as rebuild-triggering, rating changes deferred, SERP-absent products into `serpAbsentAsins`. Two DB migrations applied (product_alerts.severity, tsa_products.source_image_url). `ProductRefreshJob` extended with `diff_products` phase (DB fetch, diff, upsert with price_history rolling window, conditional `GenerateSiteJob` enqueue when `shouldRebuild && site.status='live'`) and `create_alerts` phase (unavailable/category_empty/site_degraded alerts with check-before-insert dedup). `GenerateSiteJob` writes `source_image_url` baseline. 10 unit tests pass. All builds and typechecks exit 0. Worker boots clean. End-to-end runtime proof + dedup live verification deferred to human UAT.
M006/S03 complete: Global `/alerts` page (open product_alerts with sites+tsa_products join), `acknowledgeAlert`/`resolveAlert` server actions (structured { ok, error? } return), `AlertList` client component with per-row `useTransition` isolation, Alerts nav entry (between Finances and Settings), dashboard Open Alerts KPI card with amber border+text when count > 0, `SiteAlerts` per-site component in site detail reusing same server actions. Build exit 0, typecheck exit 0. **M006 Product Refresh Pipeline milestone complete.** Human UAT pending: live DataForSEO credentials required to prove end-to-end refresh → alert creation → resolution flow.
M007/S01 complete: `@anthropic-ai/claude-agent-sdk@^0.2.76` installed + externalized in tsup + Next.js serverExternalPackages. DB migration: `agent_session_id text` added to `chat_conversations` (applied live). `ClaudeSDKClient.streamQuery()` bridges Agent SDK async iterator to typed SSE events (text/done/error). `createMonsterMcpServer(supabase)` with 4 read-only MCP tools (getPortfolioStats, getSiteDetail, getAnalytics, getAlerts) using `createSdkMcpServer` + `tool()` helpers. `/api/monster/chat` POST Route Handler: SSE bridge + conversation/message persistence + agent_session_id update + X-Conversation-Id header. Monster Chat UI: two-column layout (ConversationList sidebar + ChatWindow SSE client), server actions for CRUD, URL updated via replaceState after first turn. Streaming verified in browser: tokens appear progressively, response references real site count from DB (MCP tool call confirmed). Session resume verified: agent retains context across turns. All builds + typechecks exit 0. Key deviation: D100 superseded (string prompt correct; MCP via options.mcpServers).
M007/S02 complete: `research_sessions.progress jsonb` column added (live Supabase). `DataForSEOClient` extended with `keywordIdeas()`, `serpCompetitors()`, `googleSerpResults()` using live Labs/SERP endpoints (synchronous, no task_post/poll loop). `LABS_LANGUAGE_CODE` map added (2-letter codes for Labs, separate from 4-letter `MARKET_CONFIG`). `ResearchReportSchema` Zod schema in `packages/shared` (10 fields). `createNicheResearcherMcpServer(dfsClient, spaceshipClient)` with 5 MCP tools. `NicheResearcherJob` BullMQ worker on queue `'niche-research'` with `lockDuration: 600000`; per-turn progress writes; SDKResultMessage Zod-parsed to `ResearchReport`; parse failure gracefully stored as `{ raw, error }` with `status=completed`. Worker boots with log line. `enqueueResearch` server action (insert-before-enqueue with failure recovery). Research Lab page with niche form + market selector + 5-second polling `ResearchSessionStatus` + session history list. All builds + typechecks exit 0. Real DataForSEO data in report pending human UAT with DFS credentials configured.
M007/S03 complete: `ResearchReportViewer` server component renders all 10 `ResearchReport` fields (viability score with color-coded badge ≥70/40–69/<40, summary, recommendation, keywords table, competitors list, Amazon products grid, domain suggestions with live Spaceship availability badges, "Create site from this research" CTA). `page.tsx` branches on `status === 'completed'` — `renderCompletedSession()` named async function runs `ResearchReportSchema.safeParse()` + `Promise.allSettled()` domain checks, returns discriminated union. Parse-failure fallback renders ZodError.issues + raw JSON in `<details>` block. `SiteForm` extended with `defaultValues?: { niche?, market? }` prop; `/sites/new/page.tsx` converted to async, reads `searchParams.niche`+`searchParams.market`, passes to SiteForm. All builds + typechecks exit 0. **M007 Monster Chat + Research Lab milestone complete.** Human UAT pending: live DataForSEO + Anthropic credentials required to produce a real research report with live keyword data.
M008/S01 complete: Amazon Associates CSV import + manual revenue entry fully operational. `parseAmazonCSV` handles EN + ES format (dual-language header map, BOM strip, auto-delimiter, decimal comma). `importAmazonCSV` server action: file → parse → site attribution via `affiliate_tag` exact match → upsert to `revenue_amazon` (idempotent, `onConflict: 'site_id,date,market'`) → `ImportResult` with unattributed tracking IDs surfaced as warnings. `addManualRevenue` server action follows `addCost` pattern. `RevenueSection` client component replaces "Coming soon" placeholder with CSV import card + manual entry card. Revenue History table merges `revenue_amazon` + `revenue_manual` rows sorted by date desc. `pnpm -r typecheck` exit 0, build exit 0, pm2 reload clean. Human UAT pending: upload real Amazon Associates ES CSV export.
M008/S02 complete: Finances page upgraded from a cost ledger to a full P&L system. `getDateRange` + `computePnL` pure aggregators in `lib.ts`. `FinancesFilters` `'use client'` date form (D086 pattern). `PnLExportButton` Blob CSV download (no API route, D125). `page.tsx` wired: date-filtered parallel Supabase queries (costs/revenue_amazon/revenue_manual/domains), in-memory P&L aggregation, in-memory domain expiry filter (60 days). Page sections: date filter → P&L Summary KPI card → Per-Site Breakdown table with ROI → Domain Renewals alert card (amber, conditional) → Add Cost → Cost History → Revenue Forms → Revenue History. `pnpm -r typecheck` exit 0, build exit 0, pm2 reload clean. Human UAT pending: verify P&L totals match manual sum with real data; export CSV and open in spreadsheet. **M008 Finances + Amazon Revenue milestone complete.**
M010 complete: VPS Hetzner provisioning — `scripts/setup-vps2.sh` (349-line idempotent 7-step VPS2 bootstrap: Tailscale + Caddy + sites dirs + sudoers), `scripts/lib/vps2-check.sh` (122-line reusable health checker), `scripts/setup-vps1.sh` (248-line 6-step VPS1 bootstrap: Tailscale + nvm + pnpm + pm2 + monorepo), `scripts/deploy.sh` extended with SSH + Caddy pre-flight gate. Admin `/infra` page with `InfraService` (live VPS2 health via SSH: reachability, Caddy status, disk, memory) + "Test Deploy Connection" button. Webpack externals for native SSH modules (D140). All builds + typechecks exit 0. **M010 VPS Hetzner Provisioning milestone complete.** Operator UAT pending: run scripts on actual VPS instances and verify green health indicators in `/infra`.
M012/S02 complete: ProductForm AI content fields — five editable textareas (detailed_description, pros, cons, user_opinions_summary, meta_description) replace the old read-only AI preview. `updateProduct` server action serializes pros/cons to JSONB `{pros: string[], cons: string[]}`. `edit/page.tsx` deserializes pros_cons on load. `generate-seo-text` route handles `product_all_content` field case: collect-then-parse JSON strategy populates all five textareas via SSE + useRef direct DOM mutation. Build exit 0, typecheck exit 0, all four tsa_products content columns confirmed in DB.
M012/S03 complete: CategoryForm meta_description field + Homepage SEO card in site edit page. `meta_description` textarea in CategoryForm saves to `tsa_categories.description` (D057 alias). Site edit `edit-form.tsx` has a "Homepage SEO" card with `focus_keyword` + `homepage_seo_text` controlled textarea + "Generate with AI" SSE streaming. `updateSite` saves both fields; `generate-seo-text` route extended with `homepage_seo_text` field case. `UpdateSiteState`/`UpdateSiteErrors` extracted as distinct types. Build exit 0.
M012/S04 complete: Settings page restructured into three shadcn Tabs (API Keys / AI Prompts / Deployment). DEFAULT_PROMPTS constants defined in `constants.ts` for all three agent keys. AI Prompts textareas use `agentPrompts[key] ?? defaultPrompts[key] ?? ''` fallback chain — never empty on first load. Observability log `[settings] agentPrompts loaded: N overrides` in page.tsx. shadcn tabs installed. Build exit 0, /settings 12.9 kB. Deployment tab is display-only scaffolding (save wiring deferred).
M011 complete: Hetzner Multi-VPS Infrastructure — replaced hardcoded VPS2 settings model with a real multi-server fleet. `HetznerClient` (7 methods) + `HetznerApiError` in `packages/deployment/src/hetzner.ts`. `ProvisioningService.provision()` (5-phase: SSH key register → create server → waitForBoot → bootstrapVps SSH 6-retry → DB insert) with optional SSE `onProgress` callback. `servers` table in Supabase (12 columns, RLS enabled). `RsyncService`, `CaddyService`, and `InfraService` all migrated to accept `Server` DB records; `getFleetHealth()` replaces `getVps2Health()`. `runDeployPhase()` queries `servers` table (no vps2_* settings reads). `SETTINGS_KEYS` drops vps2_* fields, adds `hetzner_api_token`. Admin `/infra` fleet dashboard with per-server health (Name, Reachable, Caddy, Disk, Memory) + "Provision New Server" form with SSE progress streaming (D108 pattern). `GET /api/infra/datacenters` + `GET /api/infra/server-types` helper routes with hardcoded fallbacks (D155). `ProvisionModal` + `ProvisionSection` RSC/client boundary isolation (D156). All builds + typechecks exit 0. **M011 Hetzner Multi-VPS Infrastructure milestone complete.** Human UAT pending: provision a real CX22 from `/infra`, verify fleet health, deploy a site end-to-end to validate R006.

## Architecture / Key Patterns

- **Monorepo:** pnpm workspaces (`apps/admin`, `apps/generator`, `packages/*`)
- **Worktrees:** development happens in git worktrees (`/home/daniel/monster-work/gsd/M001/S01`). `/home/daniel/monster/` stays on `main` = production at all times.
- **Admin runtime:** pm2 on VPS1, added to existing ecosystem (`nous` ecosystem config as reference pattern)
- **DB:** Supabase Cloud. Schema: shared `sites` table + type-specific tables (`tsa_categories`, `tsa_products`). Migrations in `packages/db/supabase/migrations/`. Types generated via `supabase gen types`.
- **Site types:** extensible from day 1. Phase 1: TSA only. Future: AdSense blogs, multi-affiliate, lead gen.
- **Site delivery:** Astro.js static sites → rsync to VPS2 → Caddy serves → Cloudflare proxies (CDN + SSL + DDoS + CF-IPCountry)
- **AI agents:** Claude Agent SDK (Monster Chat, NicheResearcher) + Claude API via BullMQ (ContentGenerator). Plan Pro, throttle-aware, upgrade to Max when throughput demands it.
- **Analytics:** vanilla JS tracking script (~2KB) → POST direct to Supabase. Country from `CF-IPCountry` header (Cloudflare). Language from `navigator.language`.
- **Queue:** BullMQ + Upstash Redis
- **Rebuild strategy:** price/availability/image changes → immediate rebuild. Rating changes → deferred to next scheduled cycle.
- **Amazon revenue:** subtags per site + manual CSV import in Phase 1. Auto-sync API in Phase 2.
- **Focus keyword:** explicit field in DB (`focus_keyword`), generated by ContentGenerator, passed to SEO Scorer.

## Capability Contract

See `.gsd/REQUIREMENTS.md` for the explicit capability contract, requirement status, and coverage mapping.

## Milestone Sequence

- [x] M001: Foundation — Monorepo, worktrees, DB schema, shared packages, admin shell on pm2
- [x] M002: Admin Panel MVP — Dashboard, Sites CRUD, Settings, Finances shell
- [x] M003: TSA Site Generator — Templates, pipeline, ContentGenerator, SEO Scorer. R004/R005/R015 validated.
- [x] M004: Deployment + Cloudflare — packages/deployment (RsyncService + CaddyService), packages/domains (CloudflareClient + SpaceshipClient), full deploy pipeline (rsync → Caddy → CF zone+A record → ssl_pending → live), DeploySiteJob + SslPollerJob, Domain Management UI with R031-gated registration. All builds exit 0. Pending: apply cf_nameservers migration to remote Supabase; enter live credentials in Settings; live deploy + NS propagation + curl CF-RAY verification (human UAT).
- [x] M005: Analytics — S01 ✅ Tracker Script + Astro Injection | S02 ✅ Analytics Dashboard | S03 ✅ Daily Aggregation Cron
- [x] M006: Product Refresh — S01 ✅ Worker Fix + Refresh Job Scaffold + Cron Scheduler | S02 ✅ Diff Engine + Conditional Rebuild + Alert Creation | S03 ✅ Dashboard Alert Surface + Alert Resolution UI
- [x] M007: Monster Chat + Research Lab — S01 ✅ Streaming Chat + MCP Tools | S02 ✅ NicheResearcher BullMQ + DataForSEO | S03 ✅ Research Report UI + Domain Suggestions + Create Site CTA
- [x] M008: Finances + Amazon Revenue — S01 ✅ Amazon CSV Import + Manual Revenue Entry | S02 ✅ P&L Dashboard + Domain Expiry Alerts + CSV Export
- [x] M009: UX Polish + Capabilities Upgrade — Generate Site feedback, chat markdown, global chat sidebar, AI SEO generation, prompt editor, Amazon scraper, SEO files, IndexNow ping, legal templates, dashboard enhancements
- [x] M010: VPS Hetzner Provisioning — Reproducible VPS provisioning (setup-vps2.sh 7-step + setup-vps1.sh 6-step), admin /infra health dashboard (live VPS2 metrics via SSH), deploy.sh pre-flight gate. **M010 complete.**
- [x] M011: Hetzner Multi-VPS Infrastructure — S01 ✅ HetznerClient (7 methods) + ProvisioningService + servers table (Supabase) | S02 ✅ Services migration + Settings cleanup (RsyncService/CaddyService/InfraService read from servers table; vps2_* removed; hetzner_api_token added; fleet health; all builds clean) | S03 ✅ SSE provision handler + GET helper routes + ProvisionModal + ProvisionSection + /infra fleet dashboard end-to-end. **M011 complete. Human UAT (live Hetzner provision) deferred post-merge.**
- [ ] M012: Admin Polish + Mobile-First Sites — S01 ✅ DB migrations (homepage_seo_text, meta_description, tsa/classic slugs) | S02 ✅ ProductForm five editable AI content textareas + updateProduct JSONB serialization + Generate with AI → product_all_content SSE route | S03 ✅ CategoryForm meta_description field + site edit Homepage SEO card (focus_keyword + homepage_seo_text + Generate with AI) | S04 ✅ Settings three-tab layout (API Keys / AI Prompts / Deployment) + DEFAULT_PROMPTS constants (textareas never empty) | S05 ✅ Legal templates seed (8 rows, 4 types × ES+EN) + marked pipeline in generator ([legal].astro set:html) + TemplateForm Preview toggle + placeholder hint panel | S06 pending Mobile-first templates
