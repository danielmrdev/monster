# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Site type extensibility | Shared `sites` table + type-specific tables (`tsa_categories`, `tsa_products`, `blog_posts`, etc.) joined by `site_id` | Type-safe, queryable, no JSONB chaos. Dashboard/Analytics work across types without knowing internals. | No |
| D002 | M001 | arch | Admin panel runtime | pm2 on VPS1, added to existing ecosystem (nous/better-copilot pattern) | VPS1 already runs pm2. Consistent with existing processes. No Docker overhead for a single-user private panel. | Yes — if containerization needed for isolation |
| D003 | M001 | arch | Development workflow | Git worktrees at `/home/daniel/monster-work/gsd/<M>/<S>`. Main repo at `/home/daniel/monster/` stays on `main` = production. | Repo lives on production VPS. Worktrees enable development without contaminating main. | No |
| D004 | M001 | arch | DNS + CDN + SSL | Cloudflare free tier proxying all generated sites. Spaceship registers domain, NS delegated to Cloudflare. | CDN + DDoS + SSL + CF-IPCountry for analytics, all at $0. Fully automatable via Cloudflare API. Resolves DNS propagation + SSL timing issues. | Yes — if Cloudflare TOS becomes a problem |
| D005 | M001 | arch | Site deployment state machine | Sites track states: `generating → deploying → dns_pending → ssl_pending → live → paused`. Worker polls Cloudflare API for SSL readiness. | DNS propagation + CF SSL issuance is async. State machine prevents false "live" status. | No |
| D006 | M001 | data | Focus keyword storage | Explicit `focus_keyword` field in DB (`tsa_categories`, `tsa_products`, `sites`). Generated by ContentGenerator. Passed explicitly to SEO Scorer. | Explicit is auditable and editable. Inferred keywords are brittle. | No |
| D007 | M001 | arch | AI plan strategy | Start with Anthropic Plan Pro ($20/mo). ContentGenerator designed throttle-aware (BullMQ rate limiting, exponential backoff). Upgrade to Max when generation throughput is the bottleneck. | Saves $80-180/mo in Phase 1. Throttle-aware design means Pro is sufficient for <50 sites. | Yes — upgrade trigger: >50 sites or frequent rate limit errors |
| D008 | M001 | arch | Product refresh rebuild trigger | Price/availability/image changes → immediate rebuild. Rating/review changes → deferred to next scheduled cycle. | Conversion-critical data (price, stock) refreshes fast. Vanity metrics (ratings) don't justify extra builds. | Yes — can tune thresholds per site |
| D009 | M001 | arch | Amazon Associates revenue tracking | Phase 1: subtags per site (format: `<tag>-<siteslug>-20`) + manual CSV import in Finances panel. API auto-sync in Phase 2. | PA-API reporting access for ES can take weeks. Manual CSV sufficient for Phase 1 scale. | Yes — Phase 2: replace with API sync |
| D010 | M001 | scope | Phase 1 market | ES only (Amazon.es). Architecture supports all 10 Amazon markets. | Validate pipeline with one market first. Adding US/UK is a config change, not a rebuild. | Yes — expand to US/UK in Phase 2 |
| D011 | M001 | arch | Analytics country detection | Phase 1: `CF-IPCountry` header from Cloudflare (free, server-injected) + `navigator.language` for language. No separate geolocation service needed. | Cloudflare already in the stack. CF-IPCountry is free and requires zero extra code. | No |
| D012 | M001 | data | Monorepo structure | pnpm workspaces. `apps/admin` (Next.js 15), `apps/generator` (Astro.js). `packages/db`, `packages/shared`, `packages/agents`, `packages/analytics`, `packages/domains`, `packages/seo-scorer`, `packages/deployment`. | Matches existing README and CLAUDE.md conventions. pnpm workspaces without Turborepo to keep it simple. | Yes — add Turborepo if build times become an issue |
| D013 | M001 | data | Supabase schema location | Migrations in `packages/db/supabase/migrations/`. Types generated to `packages/db/src/types/supabase.ts` via `supabase gen types`. | Centralizes all DB concerns. Types importable across all packages/apps. | No |
| D014 | M001 | tooling | pnpm workspace list command | Use `pnpm ls -r --depth 0 --json` to enumerate workspaces. Plain text `pnpm ls -r` emits no output in pnpm 10 when packages have zero dependencies — JSON form is reliable regardless of dep count. | pnpm 10 changed text list behavior for zero-dep packages. JSON is stable. | No |
| D015 | M001/S02 | data | analytics_events partitioning | Implemented as regular table with cron cleanup (Phase 2). No Postgres partitioning in Phase 1. | Partitioning adds operational complexity (partition pruning, maintenance, migration tooling). Regular table is sufficient until event volume exceeds ~100M rows. | Yes — add partitioning in Phase 2 when volume warrants |
| D016 | M001/S02 | data | updated_at maintenance strategy | `updated_at` fields set in application code (S03 client layer), not via `moddatetime` extension or DB trigger. | Avoids extension dependency. Keeps DB schema simple. Application layer already controls all writes; setting updated_at there is natural and auditable. | Yes — add trigger if direct SQL writes become common |
| D017 | M001/S02 | data | product_alerts status enum | Three states: `open → acknowledged → resolved`. Plan implied only open/resolved. | Acknowledged state is operationally useful — distinguishes "I've seen this" from "I've fixed this". No downstream cost, richer state machine. | No |
| D018 | M001/S03 | tooling | Package build tool for shared packages | tsup for emit (ESM-only); `tsc --noEmit` for type-check only. Plain `tsc` emit avoided. | tsup handles extension resolution, dts generation, and tree-shaking without requiring explicit `.js` extensions in every import. Plain tsc with NodeNext requires explicit `.js` suffixes on imports — fragile and tedious. `packages/db` and `packages/shared` both use this pattern. | Yes — revisit if tsup has issues with complex re-exports |
| D019 | M001/S03 | arch | packages/db boundary — no Next.js imports | `packages/db` provides browser client factory and service-role client only. Server component clients with cookie handlers go in `apps/admin/src/lib/supabase/` (S04). | Keeping `packages/db` free of Next.js dependency prevents pollution of the dependency graph. `packages/db` can then be used by any future app or package without pulling in Next.js. | No |
| D020 | M001/S03 | arch | Workspace consumer tsconfig pattern | Apps consuming workspace packages use `"moduleResolution": "Bundler"` in tsconfig. No manual `paths` entries needed — pnpm workspace symlinks + correct `exports` map in each package resolves `@monster/*` imports via node_modules. | `paths` entries duplicate information already in the exports map and break if the dist structure changes. Bundler resolution follows the exports map directly. | No |
| D021 | M001/S03 | arch | Env var read location in client factories | `createBrowserClient()` and `createServiceClient()` read `process.env.*` inside the factory function body, not at module scope. | Reading at module scope causes imports to fail when the env var is absent. Call-time reading allows importing the package in any environment; the error surfaces only when the factory is actually invoked — making it easy to trace in server logs. | No |
| D022 | M001/S04 | tooling | shadcn v4 init prerequisites | shadcn v4 (`shadcn@latest`, version 4.x) requires: (1) `tailwindcss` as an explicit devDep (not just the postcss plugin), (2) `@/*` path alias in tsconfig, and (3) `--defaults` flag to suppress interactive prompts (not `-y`). Validation fails before writing any files if these are absent. | Documented for T02/T03 and future apps. shadcn v4 also uses Base UI (`@base-ui/react`) rather than Radix UI primitives — the "Radix" preset name in the shadcn 4.x CLI refers to the Base UI layer, not `@radix-ui/*` packages. | No |
| D023 | M001/S04 | tooling | env var loading in monorepo Next.js app | `apps/admin/.env.local` is a symlink to `../../.env` (monorepo root). Next.js 15 only reads `.env*` files in the app directory — it does not traverse up the monorepo. Symlink is covered by the root `.gitignore` pattern `.env.*`. | Avoids duplicating secrets. All secrets stay in one place (root `.env`). Symlink is transparent to Next.js — it reads the linked file normally. | Yes — alternative: copy relevant vars into a checked-in `.env.local.example` and require manual copy on new environments |
| D024 | M001/S04 | arch | shadcn v4 has no `form` component in registry | `shadcn@latest add form` returns no files in shadcn v4. The `Form` component (react-hook-form wrapper) exists in shadcn v2/v3 but not v4. Login form uses a native HTML `<form action={serverAction}>` with shadcn `Input`/`Label`/`Button` primitives directly — no react-hook-form wrapper needed for server action forms. | Server actions accept `FormData` natively; react-hook-form adds nothing for a simple 2-field login form. Simpler is correct here. | No |
| D025 | M001/S05 | tooling | pm2 script path for Next.js | `ecosystem.config.js` must use `script: 'node_modules/next/dist/bin/next'` not `'node_modules/.bin/next'`. The `.bin/next` shim is a POSIX shell script; pm2 runs the script as a Node.js file and gets `SyntaxError`. The `dist/bin/next` path is the actual JS entrypoint. | Root cause confirmed by pm2 error logs. Applies to any pm2-managed Next.js app. | No |
| D026 | M001/S05 | tooling | Next.js production build required for pm2 — dev builds crash in edge sandbox | A `next dev`-generated `.next/` dir uses `eval()` in the middleware webpack bundle. pm2 runs `next start` (production mode), which executes middleware in the V8 edge runtime sandbox where `eval()` / `new Function()` is blocked — resulting in `EvalError: Code generation from strings disallowed`. Fix: always run `pnpm build` (not `next dev`) before `pm2 start`. The deploy script (`deploy.sh`) enforces this via `pnpm -r build`. | Detected in S05 verification. The `__NEXT_BUILD_ID: "development"` in `middleware-manifest.json` is the diagnostic signal for a dev build. | No |
| D027 | M002 | arch | `SiteCustomization` Zod schema location | Canonical schema defined in `packages/shared/src/types/customization.ts` (not in `apps/admin`). Fields: primaryColor, accentColor, fontFamily, logoUrl, faviconUrl — all optional strings. | M003 Astro templates consume this shape. Defining it in `packages/shared` makes it importable by both `apps/admin` (validation) and `apps/generator` (template rendering) without a circular dependency. Defining it only in `apps/admin` would force M003 to re-invent or copy-paste the shape. | No |
| D028 | M002 | arch | Settings storage format | API keys stored as `{"value": "actual-key-here"}` JSON in the `settings.value` JSONB column. Read/write: `(row.value as { value: string }).value`. UI displays last 4 chars only. | JSONB column is typed as `Record<string, unknown>` in generated types. A wrapper object makes the read/write contract explicit and allows future metadata fields (e.g. `{"value": "...", "addedAt": "..."}`) without a schema migration. | No |
| D029 | M002 | arch | NavSidebar active state — client component boundary | A `NavItem` client component wraps each nav link and uses `usePathname()` for active state highlighting. The parent `NavSidebar` stays a server component. | Converting the entire sidebar to a client component would unnecessarily hydrate the logout form action. A small client boundary (`NavItem`) is the minimal correct fix. | No |
| D030 | M002 | scope | Finances shell scope | S04 implements cost entry form + cost list only. Revenue section is a static "Coming soon" placeholder. Amazon Associates revenue sync deferred to M008 per R012. | Phase 1 cost tracking is the operational priority — knowing what you're spending. Revenue tracking requires PA-API access approval (can take weeks) and is not blocking for Phase 1 validation. | No |
| D031 | M002/S01 | arch | `createSite` / `updateSite` server action error return vs throw | Validation errors return `{ errors: ... }` (not throw) so the form page can render inline messages. DB errors throw (surface via Next.js error boundary / pm2 logs). | Throwing on validation would crash the page and lose form state. Throwing on DB errors is correct — they're unexpected and should be surfaced visibly. | No |
| D032 | M002/S01 | arch | `updateSite` id passing via `.bind(null, id)` | Site id passed to `updateSite` via `action.bind(null, id)` in the edit page, not via a hidden `<input type="hidden" name="id">`. | Hidden inputs can be tampered with client-side. `.bind()` bakes the id into the action at render time on the server — the client cannot override it. Safer for a server action that writes to DB. | No |
| D033 | M002/S01 | arch | NavItem active state sub-route logic | Active when `pathname === href` OR (`href !== '/dashboard'` AND `pathname.startsWith(href)`). The `/dashboard` exception prevents the dashboard link from matching every sub-route. | `startsWith` ensures `/sites/new` and `/sites/[id]` both highlight "Sites". Excluding `/dashboard` from the startsWith check prevents it matching `/dashboard/...` (no such routes today but guards against future additions). | No |
| D034 | M002/S03 | arch | `'use server'` files can only export async functions — constants go in a sibling file | Next.js enforces that `'use server'` files export only async functions. Exporting a `const` array (e.g. `SETTINGS_KEYS`) from a `'use server'` file causes a build error: "A use server file can only export async functions, found object." Pattern: put shared constants in a sibling `constants.ts` (no directive) and import from both the action file and the page. | Build error confirmed during S03/T01. Applicable to any future server action file that needs to share constants with a server component. | No |
| D035 | M003 | arch | Astro data injection pattern | Per-site data serialized to `apps/generator/src/data/<slug>/site.json` before calling `build()`. Astro templates read via `getStaticPaths()`. No runtime config passed to `build()`. | Astro's build is a static snapshot of `src/`. The only reliable way to inject per-site data without re-architecting Astro's static model is to write JSON files before each build invocation. Attempting to pass runtime variables through `build()` config is not supported for per-page data. | No |
| D036 | M003 | arch | Multi-site build concurrency | BullMQ `generate` queue set to concurrency=1 (one site build at a time per worker). `teardownCompiler: false` passed in `BuildOptions` for sequential builds in the same process; teardown only on the last build. | Astro's programmatic build shares project root temp dirs. Concurrent builds for different sites would race on shared Astro internals. Serializing builds eliminates the race at acceptable throughput for Phase 1. | Yes — revisit if generation queue depth becomes a bottleneck |
| D037 | M003 | arch | "Generate Site" trigger placement | "Generate Site" button in admin panel site detail page (M003/S01). Dispatches BullMQ job. Job progress surfaced via polling of `ai_jobs` table. Real-time via Supabase subscriptions deferred to M004. | RC-M003-03 confirmed in scope for M003. Every slice must be demoable via the admin panel, not just CLI. Basic `ai_jobs` polling requires no new infrastructure — table already exists from M001 schema. | No |
| D038 | M003 | arch | ContentGenerator writes focus_keyword to DB | `ContentGenerator` generates `focus_keyword` for each category and product and writes it back to `tsa_categories.focus_keyword` / `tsa_products.focus_keyword` in Supabase as part of the content generation step, before the Astro build runs. | RC-M003-02 confirmed in scope. SEO Scorer reads focus_keyword from DB (via the JSON data file injected into Astro). Without explicit writes, the scorer has no keyword to score against. This is not optional for R005 validation. | No |
| D039 | M003 | arch | SEO Scorer page type awareness | `scorePage()` accepts `pageType: 'homepage' | 'category' | 'product' | 'legal'`. Threshold weights and factor applicability differ by page type (product pages checked for Product schema, legal pages exempted from keyword density). | Legal pages with low keyword density should not fail the scorer. Product pages should be penalized for missing structured data. Homepage has different content-length expectations than category pages. One unified threshold would produce false negatives/positives. | No |
| D040 | M003/S01 | arch | BullMQ Redis backend — Upstash via ioredis | BullMQ backed by Upstash Redis using ioredis with `enableOfflineQueue: false`, `maxRetriesPerRequest: null`. Upstash Redis credentials (`UPSTASH_REDIS_URL`, `UPSTASH_REDIS_TOKEN`) read from `.env`. | No local Redis running on VPS1. Upstash is the existing managed choice (per CLAUDE.md). ioredis is BullMQ's native client. `maxRetriesPerRequest: null` required by BullMQ for blocking commands. | Yes — switch to local Redis if latency becomes an issue |
| D041 | M003/S01 | arch | Astro outDir driven by `SITE_SLUG` env var | `astro.config.ts` sets `outDir: \`.generated-sites/\${process.env.SITE_SLUG}/dist\`` . Worker sets `process.env.SITE_SLUG` before calling `build()`. | Single Astro project root serves all sites without directory conflicts. `SITE_SLUG` is the minimal surface — no per-site config object needed in `BuildOptions`. Worker owns `SITE_SLUG` mutation; config file is stateless. | No |
| D042 | M003/S01 | arch | `packages/agents` worker runs as standalone process | `GenerateSiteJob` worker is a standalone Node.js process (`node dist/worker.js`), not embedded in the Next.js admin process. pm2 ecosystem entry deferred to S04. | Embedding a long-running BullMQ worker in a Next.js server process ties worker lifecycle to HTTP request handling — crashes in the worker could take down the admin. Separate process is correct. D002 (pm2) already assumes this pattern. | No |
| D043 | M003/S01/T01 | tooling | Astro 6 + `@tailwindcss/vite` instead of Astro 5 + `@astrojs/tailwind` | `apps/generator` uses Astro 6.0.4 (latest stable) with `@tailwindcss/vite` as a Vite plugin. `@astrojs/tailwind` (the legacy integration) is not used — it only supports Tailwind v3. | Plan referenced "Astro 5 + `@astrojs/tailwind`", but Astro 6 was released before execution started. `@astrojs/tailwind` has never supported Tailwind v4; the correct v4 integration is `@tailwindcss/vite` added directly in `astro.config.ts` under `vite.plugins`. Monorepo already uses tailwindcss@4 in apps/admin — consistency across apps is correct. | No |
| D044 | M003/S01/T02 | arch | `index.astro` reads SITE_SLUG directly in frontmatter (no `getStaticPaths`) | The homepage (`index.astro`) reads site data in the frontmatter scope (`loadSiteData(slug)`) rather than via `getStaticPaths()`. Dynamic routes (`[slug].astro`) still use `getStaticPaths()` as planned. | `getStaticPaths()` is only valid for dynamic routes with params (e.g. `[slug].astro`). For static routes like `index.astro`, Astro ignores returned props from `getStaticPaths()` — `Astro.props` remains undefined. Frontmatter data loading at build time is the correct Astro SSG pattern for non-parameterized pages. | No |
| D045 | M003/S01/T02 | arch | `loadSiteData()` uses `process.cwd()` not `import.meta.url` | `src/lib/data.ts`'s `loadSiteData()` constructs the JSON path as `join(process.cwd(), 'src', 'data', slug, 'site.json')`. Uses `process.cwd()` (the generator project root) rather than `dirname(fileURLToPath(import.meta.url))`. | Astro bundles `getStaticPaths()` into prerender chunks under `.generated-sites/<slug>/dist/.prerender/`. `import.meta.url` in that chunk points to the dist directory, making the `src/data/` path invalid. `process.cwd()` is always the generator project root during `astro build`, regardless of where the chunk file lives. | No |
| D046 | M003/S01/T02 | arch | `LEGAL_PAGES` constant defined inside `getStaticPaths()` in `[legal].astro` | The legal page definitions array is declared inside `getStaticPaths()`, not at module scope, so the Vite bundler includes it in the prerender chunk. | Vite's bundler splits module-scope constants into non-prerender chunks when they aren't directly referenced by `getStaticPaths()`. Constants at module scope that are used in both `getStaticPaths()` and the component render function get split across chunks, making them unavailable in the prerender context. Defining the constant inside `getStaticPaths()` ensures it's colocated in the prerender bundle. | No |
| D047 | M003/S01/T03 | tooling | tsup DTS disabled for `packages/agents`; hand-written `dist/index.d.ts` | tsup's DTS build (rollup-plugin-dts) resolves both ioredis@5.10 and bullmq's pinned ioredis@5.9.3, causing a type incompatibility on `AbstractConnector.connecting` (protected member, two separate class hierarchies). Fixed by disabling DTS and writing a minimal `dist/index.d.ts` by hand. pnpm override `"ioredis": "5.9.3"` added to root package.json. | The override doesn't fully deduplicate at the pnpm store level; it only affects resolution for packages that don't pin a version. If a future ioredis upgrade re-introduces the conflict, re-evaluate DTS or upgrade bullmq first. | Yes — re-enable DTS when ioredis versions align |
| D048 | M003/S01/T03 | arch | `GenerateSiteJob` not exported from `packages/agents` index | `index.ts` exports only `generateQueue()` and Redis helpers. `GenerateSiteJob` is internal to `worker.ts`. | Next.js webpack tried to bundle `packages/agents/dist/index.js` into the admin bundle. `GenerateSiteJob` transitively imports `astro`, which uses `data:` scheme imports that webpack cannot resolve. Removing `GenerateSiteJob` from the public index keeps the admin bundle clean. Worker imports it directly at runtime. | No |
| D049 | M003/S01/T03 | arch | `process.chdir(GENERATOR_ROOT)` before Astro `build()` call | Worker calls `process.chdir(GENERATOR_ROOT)` before `await build({ root: GENERATOR_ROOT })` and restores the previous cwd in a `finally` block. | `loadSiteData()` in the generator uses `process.cwd()` to resolve `src/data/<slug>/site.json`. When the worker runs from the monorepo root, `process.cwd()` is `/home/daniel/monster`, not `apps/generator`. Passing `root` to `build()` changes Astro's internal path resolution but does not change `process.cwd()` for user-code running inside getStaticPaths(). | Yes — if multiple concurrent builds run in the same process, they would race on process.cwd(). Concurrency=1 (D036) prevents this. |
| D050 | M003/S02/T01 | arch | DataForSEO credentials read from Supabase `settings` table, not `.env` | `DataForSEOClient.fetchAuthHeader()` reads the `dataforseo_api_key` setting from Supabase at call time. Credential format: `email:password` stored as `{"value": "email:password"}` JSON (D028 pattern). Base64-encoded to `Authorization: Basic` header. | Consistent with D028 (settings storage format). API credentials entered via admin Settings UI — no VPS env var management required. Email-only portion safe to log for diagnostics; full credential must never appear in logs. | No |
| D051 | M003/S02/T01 | arch | `tasks_ready` polling uses cast `(result as unknown as { id?: string }).id` | DataForSEO `tasks_ready` endpoint returns result entries with `id` field; `DFSRawResult` interface (typed for `task_get` results) doesn't include `id`. Cast avoids defining a parallel interface for the polling-only shape. | Two separate response shapes share the same generic wrapper. A separate `DFSReadyResult` interface would be correct but adds ceremony for a one-field difference used only in the polling loop. If DataForSEO adds more ready-specific fields, promote the cast to a proper type. | Yes — promote to proper interface if `tasks_ready` response shape gains more used fields |
| D052 | M003/S02/T02 | arch | Amazon CDN blocks non-browser User-Agent; image pipeline handles gracefully | `m.media-amazon.com` and `ssl-images-amazon.com` return 403/404 for Node.js default fetch headers. `downloadAndConvertImage` returns `false` (non-fatal), `tsa_products.images` gets `[]`, product page renders without image. No User-Agent spoofing in Phase 1. | Adding a browser User-Agent string is a one-line fix but creates a maintenance dependency on a specific UA string. Products without images degrade gracefully. S03 ContentGenerator can still generate text content for imageless products. Revisit when real image coverage matters for CTR. | Yes — add `User-Agent: Mozilla/5.0 ...` header in fetch call if image coverage drops below acceptable threshold |
| D053 | M003/S02/T02 | arch | `astro.config.ts` `publicDir` driven by `SITE_SLUG` env var | `publicDir: \`.generated-sites/${slug}/public\`` mirrors the `outDir` pattern (D041). Astro copies everything in `publicDir` into `dist/` at build time. Worker creates `publicDir/images/products/` before calling `processImages()`. | Per-site isolation: each site's downloaded images live under its own slug directory and are copied to its own `dist/`. Without per-site `publicDir`, images from site A would appear in site B's build output. Pattern is symmetric with `outDir` (same slug, different subdirectory). | No |
| D054 | M003/S02/T03 | arch | `imageUrl` kept in memory only — never written to `tsa_products.images` | DataForSEO `imageUrl` (Amazon CDN URL) is stored only in the in-memory `DataForSEOProduct` object and passed to `processImages()`. `tsa_products.images` is written with `[]` on initial upsert and updated only with local `/images/products/<asin>-0.webp` paths after image download. | Amazon CDN URLs expire and their inclusion in the DB would create phantom image references. The rule "no CDN URLs in built HTML" (S02 must-have) is enforced structurally: `SiteData` is assembled from DB rows after upserts, and `tsa_products.images` only ever contains local paths. | No |
| D055 | M003/S02/T03 | arch | Secondary DataForSEO keyword (`accesorios ${niche}`) is non-fatal | If the second `searchProducts()` call throws, the job logs the error and continues with a single category. Only the primary keyword is required. | A second category is a UX enhancement, not a pipeline requirement. Failing the whole job because "accesorios freidoras de aire" returned zero results would be wrong — the primary niche products are already fetched. Zero categories after the primary fetch is the only fatal condition. | No |
| D056 | M003/S02/T03 | arch | `SiteData` assembled from fresh DB rows after upserts | After all Supabase upserts complete, `GenerateSiteJob` re-fetches `tsa_categories`, `tsa_products`, and `category_products` from Supabase to build `SiteData`. Does not use in-memory DataForSEO objects. | Ensures `site.json` reflects exactly what is in the database — including any fields modified by the upsert (e.g. updated_at). Prevents inconsistency between what was written to DB and what was built into the site. Also ensures idempotent re-runs produce the same `site.json` as the original run. | No |
| D057 | M003/S03 | arch | `category.meta_description` stored in `tsa_categories.description` column | `ContentGenerator.generateCategoryContent()` returns `meta_description`; it is written to `tsa_categories.description` (existing nullable text column). No new DB column needed. | `tsa_categories` has no `meta_description` column but has `description: string | null` which is semantically equivalent for category pages. Avoids a migration. `SiteData` maps `cat.description` → `CategoryData.meta_description` in assembly. | Yes — add explicit `meta_description` column if description needs a different value |
| D058 | M003/S03 | arch | Product `meta_description` passed in-memory only — not persisted to DB | `ContentGenerator.generateProductContent()` returns `meta_description`; it is stored in a `Map<productId, string>` in `generate_content` phase and injected into `SiteData` assembly. Not written to any DB column. | No `meta_description` column exists in `tsa_products`. Product content (description, pros_cons, user_opinions_summary) IS idempotent. `meta_description` is the only field re-generated each run — acceptable for Phase 1. S04 (SEO Scorer) reads from built HTML, not DB directly. | Yes — add `meta_description` column in a migration if idempotency of meta description becomes required |
| D059 | M003/S03 | arch | BullMQ Worker `lockDuration: 300000` (5 minutes) for ContentGenerator phase | Default BullMQ lock duration is 30s. ContentGenerator adds 60s+ to job execution (20 products × 1.5s sleep + API roundtrip). Without extending the lock, BullMQ considers the job stalled and reschedules it mid-generation. | 5 minutes covers worst-case content generation (30 products × ~3s each = 90s) with substantial headroom. ContentGenerator runs sequentially (D036 — concurrency=1) so there's no parallelism to exploit for a tighter bound. | Yes — reduce if generation becomes faster; increase if product count grows beyond 30 |
| D060 | M003/S03/T01 | tooling | `zodOutputFormat` imported from `@anthropic-ai/sdk/helpers/zod` subpath | The function is not re-exported from the main `@anthropic-ai/sdk` index. It lives at `@anthropic-ai/sdk/helpers/zod`. The task plan said `import { zodOutputFormat } from '@anthropic-ai/sdk'` — the actual import path is the helpers subpath. No named second argument (schema name) — `zodOutputFormat(schema)` takes only the schema. | Verified by inspecting the installed SDK's `index.d.ts` (no zodOutputFormat export) and `helpers/zod.d.ts` (exported from there). The function uses `z.toJSONSchema()` internally, confirming Zod v4 compatibility only. | No |
| D061 | M003/S04/T01 | tooling | `text-readability` default-instance import + hand-authored .d.ts | `text-readability` exports a single default Readability class instance (not named exports). Import as `import readability from 'text-readability'` then call `readability.fleschReadingEase(text)`. No @types package exists — hand-wrote `src/text-readability.d.ts` declaring the instance interface. | Checked the library's `main.js` — last line is `export default readability` (an instance). Named import would fail at runtime. DTS build requires the declaration; without it tsup strict DTS exits 1. | No |
| D062 | M003/S04/T01 | arch | Legal page SEO: keyword density/first-para exemptions award full marks (not zero) | For `pageType === 'legal'`, the content_quality scorer awards full marks for the keyword density sub-score and first-paragraph sub-score rather than skipping them as 0. Legal pages score on word count + readability only. | Skipping as 0 would artificially penalize well-written legal pages that have good word count and readability — the exemption is intended to remove keyword-centric penalties, not impose content penalties. A 500-word well-structured legal page should receive a decent score. | No |
| D063 | M004 | arch | Caddy config: file-based per-site snippets, not JSON API | Write `/etc/caddy/sites/<domain>.caddy` over SSH and run `sudo systemctl reload caddy`. Not the Caddy JSON API (`POST /load`, port 2019). | File-based config survives VPS2 reboots without maintaining in-memory state. Auditable as files. No extra network port exposure over Tailscale. Caddy gracefully hot-reloads — no downtime on reload. Matches standard nginx/Apache sysadmin pattern. | Yes — revisit if Caddy API becomes necessary for dynamic reconfiguration at scale |
| D064 | M004 | arch | Cloudflare integration via `cloudflare` npm package (v5+), not raw fetch | Use `cloudflare` npm package for all Cloudflare API calls: `client.zones.create()`, `client.dns.records.create()`, `client.ssl.verification.get()`. Not raw `fetch()`. | Full TypeScript types, consistent error handling, auto-pagination. Avoids re-implementing auth headers, retry logic, and response parsing for CF's complex API surface. | No |
| D065 | M004 | arch | Spaceship API via raw `fetch`, no npm client | All Spaceship API calls use raw `fetch` with `X-Api-Key` + `X-Api-Secret` headers. No npm client exists. | Well-documented REST API. The only endpoints needed (availability check, registration, NS update, async operation poll) are straightforward. A thin `SpaceshipClient` class wrapping fetch is sufficient and avoids a dependency on a non-existent or third-party client. | No |
| D066 | M004 | arch | Cloudflare zone creation is idempotent: check-then-create | `CloudflareClient.ensureZone()` calls `client.zones.list({ name: domain })` first. If a zone exists, return its id and nameservers. Only calls `client.zones.create()` if no zone found. | Retries after a failed deploy would otherwise create duplicate zones or return an error. Idempotent zone creation makes the deploy phase re-runnable without side effects. | No |
| D067 | M004 | arch | SSL poller as a separate BullMQ delayed job, not blocking the deploy worker | After transitioning to `ssl_pending`, a `SslPollerJob` is enqueued with a delay (60s). It polls `client.ssl.verification.get()` and either transitions `live` or re-enqueues itself with delay. Main deploy worker does not wait for SSL. | SSL issuance can take 1–15 minutes. Blocking the BullMQ worker thread for that duration would stall the queue and risk lock expiry even with extended `lockDuration`. Separate delayed job is the correct BullMQ pattern for long async waits. | No |
| D068 | M004 | arch | Deploy phase added to `GenerateSiteJob` after `score_pages`, not a separate job | The deploy pipeline (rsync + Caddy + CF zone + A record) runs as a `deploy` phase inside the existing `GenerateSiteJob`, following the established phase pattern. A separate `DeploySiteJob` exists only for standalone redeploys (triggered without regeneration). | Extending `GenerateSiteJob` avoids a second job definition for the common generate+deploy flow. Separate `DeploySiteJob` is needed for redeploy-only (e.g. after a content fix without a full regeneration). Both share `RsyncService` and `CloudflareClient`. | Yes — if deploy is always decoupled from generation, merge both into one job type |
| D069 | M004 | arch | Amazon CDN User-Agent fix lands in S01 (pre-deploy prerequisite) | `downloadAndConvertImage()` in `packages/agents/src/pipeline/images.ts` gets `'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'` added to fetch headers before M004/S01 wires the deploy. | Deploying sites with empty `images[]` arrays would produce product pages with no images — lower SEO scores and poor UX. The fix is one line, low risk, and must precede any real site deploy. Documented in M003 forward intelligence as D052 revisit trigger. | No |
| D070 | M004/S01 | arch | `RsyncService` uses `child_process.spawn` (rsync CLI); `CaddyService` uses `node-ssh` | RsyncService spawns the local `rsync` binary with `-e "ssh -o StrictHostKeyChecking=no"` — rsync handles SSH transport natively, no separate SSH session needed. CaddyService uses `node-ssh` to SSH into VPS2, write the Caddyfile snippet via `tee`, and reload Caddy via `systemctl`. | Rsync CLI is the standard way to rsync over SSH — no need to re-implement the transfer protocol. CaddyService needs two SSH operations (write file + exec command) that are cleaner via an SSH session object than spawning multiple processes. `node-ssh` provides a clean Promise API with SSH agent support. | Yes — revisit if Tailscale SSH auth changes |
| D071 | M004/S01 | arch | CaddyService uses SSH agent (`process.env.SSH_AUTH_SOCK`) — no key file management | `node-ssh` connects with `{ agent: process.env.SSH_AUTH_SOCK }`. No private key path is stored in settings or passed to the service. | VPS1 already has Tailscale SSH configured with agent forwarding. Storing a key file path adds operational complexity and a secrets-management concern. SSH agent is the standard pattern for interactive and automated Tailscale SSH access. | Yes — add fallback to `~/.ssh/id_rsa` if agent socket isn't set |
| D072 | M004/S01 | arch | VPS2 deployment keys (`vps2_host`, `vps2_user`, `vps2_sites_root`) stored as plain text in settings, not secrets | These three keys are stored with `type="text"` inputs in the Settings UI. `MaskedIndicator` still shows "is configured" signal (last 4 chars). | VPS2 host and user are not secrets — they're visible in Tailscale dashboard and SSH config. `vps2_sites_root` is a filesystem path. None have the security profile of an API key. Plain text inputs are less confusing than password inputs for non-secret config. | No |
| D073 | M004/S01/T01 | arch | `RsyncService` resolves monorepo root via `process.cwd()` | `process.cwd()` used to resolve `.generated-sites/<slug>/dist/`. `import.meta.url`-based resolution is not used. | All callers (BullMQ worker, test scripts) run from the monorepo root. `process.cwd()` is stable and predictable. `import.meta.url`-based resolution would point to the package dist dir (`packages/deployment/dist/`), requiring `../../../..` traversal — fragile if the output structure changes. | Yes — if callers run from a different cwd, add explicit `monorepoRoot` param to `deploy()` |
| D074 | M004/S02 | arch | `node-ssh` and `cloudflare` added to `packages/agents` tsup external list | When `@monster/deployment` and `@monster/domains` are bundled into the agents worker via `noExternal: [/@monster\/.*/]`, their transitive dependencies (`node-ssh` for CaddyService, `cloudflare` npm package for CloudflareClient) are pulled into the bundle. Both must be added to the agents tsup `external` list to avoid bundling conflicts or bloat. | `node-ssh` has native addons (via `ssh2`/`cpu-features`) that cannot be bundled. `cloudflare` is a large package (>1MB) with its own ESM structure. Externalizing both keeps the agents bundle size reasonable and avoids native addon bundling errors. | No |
| D075 | M004/S02 | arch | `domains` row upserted on deploy with `onConflict: 'domain'` | The `domains.domain` column has a UNIQUE constraint in the schema (not a composite key). Upsert uses `onConflict: 'domain'` for idempotent zone tracking. | The UNIQUE constraint is on `domain` alone. `onConflict: 'site_id,domain'` would require a composite unique index that doesn't exist. Using the correct conflict target prevents duplicate-row errors on redeploy while keeping the operation idempotent. | No |
| D076 | M004/S02/T01 | api | `cloudflare` SDK v5 `dns.records.list()` name param is a `Name` object, not a string | `RecordListParams.name` is `{ contains?, exact?, startswith?, endswith? }` — not a bare string. Use `{ exact: domain }` for exact-match DNS record lookup. The task plan pseudocode used `name: domain` (string) which fails TypeScript compilation. | Breaking change vs SDK v4. Bare string passes JS runtime but TypeScript strict mode rejects it. The `Name` object is a filter spec supporting substring/prefix/suffix matching — the CF API's query model. | No |
| D077 | M004/S02/T02 | arch | `runDeployPhase()` extracted as shared helper called from both `GenerateSiteJob` and `DeploySiteJob` | Deploy orchestration logic (rsync → Caddy → CF zone+A record → state transitions → SslPollerJob enqueue) extracted to `runDeployPhase(siteId, site, bullJobId, supabase)` in `deploy-site.ts`. Both jobs call this helper. | Avoids code duplication between the generate pipeline's deploy phase and the standalone redeploy job. `bullJobId` is optional — when `undefined` (future headless use), `ai_jobs` progress updates are skipped. Helper is exported so future job types can reuse it. | No |
| D078 | M004/S02/T02 | pattern | `SslPollerJob` treats `pollSslStatus` errors as `'pending'` and re-enqueues | When `CloudflareClient.pollSslStatus()` throws (CF API unreachable, network error), the error is caught, logged, and treated as `sslStatus = 'pending'`. The job re-enqueues unless attempt limit is reached. | CF API transient failures should not terminate the polling cycle — a temporary outage doesn't mean SSL is broken. The 30-attempt * 60s = ~30min window is generous enough to ride out brief API issues. If the limit is hit, `sites.status = 'error'` surfaces the problem for human inspection. | No |
| D079 | M005 | arch | Tracker build artifact: commit `dist/tracker.min.js` to repo, reference as inline script in BaseLayout.astro | Built tracker artifact is committed to `packages/analytics/dist/tracker.min.js`. Generator's `BaseLayout.astro` reads it from disk at Astro build time and injects as an inline `<script>` tag. No workspace dependency between `@monster/analytics` and `apps/generator`. | Inline injection eliminates an extra HTTP request and makes the byte count directly visible at build time. Committing dist avoids build-order coupling in the pnpm workspace — the generator does not need `@monster/analytics` as a dependency. The artifact is small (<2KB) so committing it is not a burden. Build it once in `packages/analytics`, commit, and the generator consumes it statically. | Yes — switch to workspace dependency if the generator needs to import tracker types |
| D080 | M005 | arch | Visitor hash: `SHA-256(date + userAgent)` via Web Crypto API (client-side), no IP component | `visitor_hash = hex(SHA-256(YYYY-MM-DD + navigator.userAgent))`. Computed client-side using `crypto.subtle.digest('SHA-256', ...)`. IP is not included — not available client-side without a geolocation service. | PRD specifies `hash(date + IP + user-agent)` server-side, but PostgREST exposes no request metadata and adding a geolocation API call violates the <2KB / no-external-dependency constraint. Client-side hash without IP is a reasonable Phase 1 approximation: provides daily deduplication within a device, is GDPR-safe (no PII stored), and requires zero extra infrastructure. `unique_visitors` in `analytics_daily` is a lower bound, not exact — documented in code comments. | Yes — add Supabase Edge Function for server-side IP-based hashing if accuracy becomes important |
| D081 | M005 | arch | Country detection: `null` for Phase 1 tracker events | `analytics_events.country` is always `null` in Phase 1. CF-IPCountry is only injected into browser→VPS2 page requests, not into browser→Supabase tracker POST requests. No client-side IP geolocation service is used. | CF-IPCountry is a server-injected header, not a value exposed to client-side JavaScript. The tracker POST goes browser→Supabase Cloud directly, bypassing Cloudflare entirely. Adding a client-side geolocation API call would violate the <2KB / no-external-dependency constraint and would constitute an external call that affects GDPR posture. `null` is acceptable for Phase 1; country analytics can be added via Edge Function in Phase 2 (R024). | Yes — R024 (deferred) describes the correct Phase 2 path |
| D082 | M005 | arch | Daily aggregation cron via BullMQ repeat job (`0 2 * * *`), not Vercel Cron | The analytics aggregation cron is implemented as a BullMQ repeat job registered on worker startup with stable jobId `'analytics-daily-aggregation'`. Not using Vercel Cron. | Admin runs on VPS1 via pm2, not on Vercel. Vercel Cron requires deployment to Vercel serverless infrastructure. BullMQ v5 supports cron-syntax repeat jobs natively with no separate QueueScheduler process. Stable jobId prevents duplicate job registration on worker restart. | No |
| D083 | M005 | arch | Aggregation SQL: full-day atomic INSERT...ON CONFLICT...DO UPDATE, not incremental | The aggregation job runs a single SQL query that aggregates all events for a given date in one pass and upserts the result into `analytics_daily`. Conflict target: `(site_id, date)`. Uses `EXCLUDED.*` to overwrite. | Incremental updates to jsonb fields (`top_countries`, `top_referrers`, `top_pages`) require complex merge logic and are prone to partial-update bugs if the job runs twice for the same day. Full-day atomic aggregation (delete-equivalent semantics via DO UPDATE) is idempotent: running the job multiple times for the same day produces the same result. Simple and correct. | No |
| D084 | M005/S01 | arch | Tracker POST transport: `fetch` with `keepalive: true`, not `navigator.sendBeacon` | The tracker uses `fetch(url, { method: 'POST', keepalive: true, headers: { apikey, Authorization, 'Content-Type': 'application/json' }, body: JSON.stringify(events) })` as the event flush transport. `sendBeacon` is not used as the primary transport. | PostgREST requires `apikey` and `Authorization` request headers for INSERT. `navigator.sendBeacon` does not support custom request headers — it only accepts a URL and a body. `fetch` with `keepalive: true` provides the same "fire and forget on page unload" guarantee while supporting custom headers. Wrapped in `try/catch` — failures are silent (fire-and-forget semantics preserved). | No |
| D085 | M005/S01 | arch | Tracker placeholder substitution: literal strings in source, string-replaced at Astro build time | The tracker source contains literal placeholder strings `__SUPABASE_URL__`, `__SUPABASE_ANON_KEY__`, `__SITE_ID__`. esbuild preserves these in the minified output. `BaseLayout.astro` reads `tracker.min.js` at Astro build time and performs string `.replace()` before injecting as `<script set:html={...}>`. | This avoids any runtime configuration mechanism in the tracker (no global vars, no data attributes on the script tag). Substitution happens at Astro SSG build time — the values are baked into each site's static HTML. The approach requires that the placeholder strings appear exactly once each in the minified output; esbuild's minification preserves string literals it cannot constant-fold. | No |
| D086 | M005/S02 | pattern | Native `<select>` elements in `AnalyticsFilters` — shadcn Select incompatible with `<form method="GET">` | `AnalyticsFilters.tsx` uses plain HTML `<select>` elements with matching Tailwind classes instead of the shadcn `Select` component. `onChange` calls `e.currentTarget.form?.submit()` for immediate navigation. | shadcn Select is a Base UI headless component: it manages its own internal state and renders a custom-styled trigger+dropdown via portals. It does not render a native `<select>` and therefore does not participate in `<form method="GET">` serialization — form submit would not include the select values. Native `<select>` preserves both params on form submit without `useRouter` or `useSearchParams`. Fallback Apply button included for keyboard/no-JS users. | No |
| D087 | M005/S03 | arch | `registerScheduler()` creates and closes its own fresh queue instance | `AnalyticsAggregationJob.registerScheduler()` creates a dedicated queue via `createAnalyticsAggregationQueue()`, calls `upsertJobScheduler(...)`, then closes the queue in a `finally` block. It does not use the `analyticsAggregationQueue()` singleton. | `upsertJobScheduler` is a one-shot startup call. Using the singleton for it would leave the connection open after registration and mix lifecycle concerns between the scheduler setup path (worker startup) and the enqueue path (admin server action). Fresh queue + close in finally is the correct pattern for a one-shot use. | No |
| D088 | M005/S03 | arch | `AnalyticsAggregationJob` aggregation keyed by `${siteId}::${pagePath}` (:: separator) | In-memory accumulator map uses `${siteId}::${pagePath}` as the key. `::` is chosen as the separator because site UUIDs contain only hex digits and hyphens — `::` cannot appear in a UUID and is therefore a collision-free separator. | Concatenating without a separator risks collisions if `pagePath` starts with a character that appears at the end of a UUID. A UUID-safe separator (`::`, `\x00`, or a known-absent character) prevents accidental key merges. `::` is readable and debuggable in logs. | No |
| D089 | M005/S03 | pattern | `AggregationTrigger` extracted as a separate `'use client'` file (not inlined in analytics `page.tsx`) | The analytics page is an async server component. `AggregationTrigger.tsx` is a separate file with `'use client'` directive, imported into `page.tsx` as a leaf component. `useTransition` and `useState` live entirely within the client component. | Mixing `'use client'` into an async server component file is a Next.js error. The RSC boundary must be at the file level. Extracting to a separate file is the canonical Next.js App Router pattern for interactive leaves within a server component tree. Applies to any future interactive element in a server-rendered analytics or dashboard page. | No |
| D090 | M005/S03 | arch | `enqueueAnalyticsAggregation` returns `{ ok, jobId, date, error }` with date included | The server action returns the queued date in the response payload in addition to the BullMQ job ID and success flag. | Returning `date` enables the inline UI status message to confirm exactly which date was queued ("Queued for 2026-03-12"), which is especially useful when the default "yesterday" is computed server-side and the user cannot otherwise see which date was targeted. No cost to include; improves debuggability. | No |
| D091 | M006 | arch | `ProductRefreshJob` detects changes and enqueues `GenerateSiteJob` — does not call Astro `build()` inline | The refresh job's responsibility ends at change detection and enqueueing. When diff engine detects rebuild-triggering changes (`price`, `availability`, `image`), it enqueues a new `GenerateSiteJob` on the existing `generate` queue (concurrency=1) rather than calling `build()` inside the refresh job. | `GenerateSiteJob` calls `process.chdir(GENERATOR_ROOT)` before `build()`. If the refresh job also called `build()` inline, concurrent runs across different queues would race on `process.cwd()`. The `generate` queue already serializes builds (D036). Decoupling detection from rebuild keeps `ProductRefreshJob` stateless w.r.t. the Astro build system and makes each piece independently debuggable. | No |
| D092 | M006 | arch | SERP-absence treated as `'limited'` availability, not `'unavailable'` — no direct unavailability alert | Products not returned in a keyword SERP are marked `available: 'limited'` in the diff result, not `'unavailable'`. Only products explicitly returned with an out-of-stock or no-listing signal (or absent from a targeted ASIN lookup in Phase 2) trigger `product_unavailable` alerts. SERP-absent products trigger no alert in Phase 1. | A keyword SERP returns Amazon's current ranking for that query — catalog rotation is normal. A product not ranking on page 1 today may rank tomorrow. Treating SERP absence as unavailability would produce a flood of false-positive alerts, particularly after Amazon algorithm updates. Phase 2 ASIN-level lookup resolves actual availability. | Yes — revisit when ASIN lookup is implemented; SERP-absent + ASIN-confirmed-unavailable = hard unavailable |
| D093 | M006 | arch | Alert deduplication: check-before-insert on `(site_id, product_id, alert_type)` where `status = 'open'` | Before inserting a new `product_alerts` row, the refresh job queries for an existing open alert with the same `(site_id, product_id, alert_type)`. If found, skips insert. No DB-level unique constraint (a resolved alert followed by a new occurrence should create a new row). | A UNIQUE constraint on `(site_id, product_id, alert_type)` would prevent re-alerting after resolution — incorrect semantics. The check-before-insert pattern allows the full `open → acknowledged → resolved → open` lifecycle. The only invariant enforced is "one open alert per problem" — not "one alert ever per problem". | Yes — add DB partial unique index `WHERE status = 'open'` if check-before-insert proves racy under concurrent refresh jobs |
| D094 | M006 | arch | `node-ssh` added as direct dep of `packages/agents` to fix ERR_MODULE_NOT_FOUND | `"node-ssh": "^13.2.1"` added to `packages/agents/package.json` dependencies. This mirrors `packages/deployment`'s declaration; pnpm dedupes in the store. The tsup external list already includes `node-ssh` (D074) — this is a runtime resolution fix, not a bundling change. | The monster-worker process runs as `node packages/agents/dist/worker.js` from the monorepo root. pnpm does not hoist `node-ssh` to the root `node_modules/` because it is only declared in `packages/deployment`. Adding it as a direct dep in `packages/agents` makes it resolvable from the worker's node_modules resolution chain. | No |
| D095 | M006 | arch | Price history stored as JSONB rolling window: read-prepend-slice-write on each refresh | `tsa_products.price_history` is updated by reading existing JSONB array, prepending `{ price, date }`, slicing to max 30 entries, and writing back via upsert. No append-only growth. | The JSONB column type is already in the schema. A rolling window cap prevents unbounded growth in the JSONB column. Max 30 entries × ~30 bytes each = ~900 bytes per product — well within Postgres JSONB performance range. Implementation is a single read-modify-write; no separate price_history table needed for Phase 1. | Yes — promote to a separate price_history table with proper indexing if historical querying becomes a feature |
| D096 | M006/S01/T01 | tooling | `cloudflare` npm package added as direct dep of `packages/agents` (same pattern as D094/node-ssh) | `"cloudflare": "^5.2.0"` added to `packages/agents/package.json`. tsup external list already included it (D074). `packages/domains` declared it originally; the worker resolution chain can't find it without a direct dep. | Same pnpm hoisting issue as node-ssh (D094). The worker runs from monorepo root; only packages in `packages/agents`'s dep graph (not siblings') are resolvable. Discovery process: fix node-ssh → new ERR_MODULE_NOT_FOUND for cloudflare → add direct dep. Pattern: any package in tsup's external list that comes from a non-agent workspace package needs to be mirrored in agents' dependencies. | No |
| D097 | M006/S01/T01 | tooling | `banner: createRequire` injected into agents worker tsup config to fix CJS builtins in ESM bundle | `banner: { js: "import { createRequire } from 'module';\nconst require = createRequire(import.meta.url);" }` added to the worker tsup config. `platform: 'node'` also added but does not alone fix the issue. | `@monster/seo-scorer` (bundled via `noExternal: [/@monster\/.*/]`) imports `cheerio` → `encoding-sniffer` → `iconv-lite` (CJS). iconv-lite calls `require('buffer')` internally. In ESM context, `require` is undefined; tsup's `__require` shim throws for Node builtins without a real `require`. The `createRequire(import.meta.url)` banner provides a real Node.js `require` that resolves builtins. This is the standard tsup fix for CJS packages with Node built-in dependencies when bundled into ESM output. | Yes — if `@monster/seo-scorer` is ever refactored to native ESM (no CJS deps), banner can be removed |
| D098 | M006/S02/T02 | tooling | `@monster/db` must be rebuilt after manual edits to `supabase.ts` | Run `pnpm --filter @monster/db build` after every manual edit to `packages/db/src/types/supabase.ts`. Downstream packages (agents, admin) resolve types from `packages/db/dist/index.d.ts` — not the source file. Skipping the rebuild causes `SelectQueryError` type failures in Supabase query builder chains. | pnpm monorepo: each package compiles to its own dist. The workspace resolution uses the compiled output declared in `package.json exports`, not raw source. Manual supabase.ts edits bypass the normal migration→generate→build flow, so the rebuild step is manual too. | No |
| D099 | M007 | arch | Monster Chat streaming: Route Handler + SSE, not WebSockets or server actions | `/api/monster/chat` Next.js Route Handler returns `new Response(stream, { headers: { 'Content-Type': 'text/event-stream', ... } })`. Agent SDK `for await` loop writes events as `data: {...}\n\n`. Client uses `fetch` with streaming body reader (not `EventSource` — EventSource is GET-only). Server actions cannot return streaming responses. | SSE via Route Handler is the canonical Next.js 15 App Router pattern for streaming AI responses. WebSockets add operational complexity (stateful connections, different deployment model). Server actions are not suitable — they buffer the full response before returning. Route Handler + fetch with streaming body reader is well-supported, simple, and stateless. | No |
| D100 | M007 | arch | Monster MCP tools: prompt must be async generator (not string) to enable MCP tool calls | When using custom MCP servers with the Agent SDK, `prompt` must be an async generator yielding user message objects (e.g. `async function* () { yield { role: 'user', content: userMessage } }`). Passing a plain string as `prompt` causes the SDK to skip MCP server initialization entirely — tools are never invoked, agents run without portfolio context. This is a documented SDK requirement. | Silent failure mode: if prompt is a string, the agent responds using only its training data, never calling portfolio tools. The diagnostic is that responses never reference real DB values. The fix is always using the async generator form when MCP servers are configured. | No |
| D101 | M007 | arch | Agent SDK (`@anthropic-ai/claude-agent-sdk`) externalized in both tsup and Next.js webpack | Added to `external` list in `packages/agents/tsup.config.ts` (both index and worker entries). Added to `serverExternalPackages` in `apps/admin/next.config.ts`. Added as direct dep in `packages/agents/package.json` (D094/D096 pattern). | SDK is 60MB unpacked — bundling it would produce an enormous worker artifact. It has complex ESM internals that webpack cannot reliably process. `serverExternalPackages` prevents admin's webpack from trying to bundle it when imported in Route Handlers. Direct dep in agents ensures it's resolvable from the worker's module resolution chain (same pnpm hoisting pattern as D094/D096). | No |
| D102 | M007 | arch | NicheResearcher progress: writes to `research_sessions.progress` jsonb per agent turn; UI polls Supabase every 5 seconds | NicheResearcher BullMQ job calls `supabase.from('research_sessions').update({ progress: currentProgress })` after each agent tool-call turn. Admin panel research page polls this column via a 5-second `setInterval` (same `JobStatus.tsx` pattern). SSE not used for research (job takes 5–10 min; SSE connections drop on proxies after ~60s). | Decoupling progress storage from the HTTP connection makes the research job resilient to browser disconnects. The UI can be closed and reopened — the session state is in DB, not memory. 5-second polling is sufficient UX resolution for a 5–10 min job. SSE is appropriate only for Monster Chat (short turns, seconds not minutes). | No |
| D103 | M007 | arch | Agent SDK session resume: `chat_conversations.agent_session_id` stores Agent SDK session ID returned from first `query()` call | On the first message in a conversation, the Route Handler stores the `session_id` field from the SDK response in `chat_conversations.agent_session_id`. Subsequent messages in the same conversation pass `resume: agentSessionId` to `query()`. The SDK handles memory server-side; admin stores only the session ID. | Avoids replaying the full conversation message history on each turn (expensive and token-costly). SDK session resume is the designed pattern for multi-turn conversations. Storing session ID in the conversation record means conversation continuity survives page reloads. The `agent_session_id` column requires a migration (`ALTER TABLE chat_conversations ADD COLUMN IF NOT EXISTS agent_session_id text`). | Yes — if SDK session resume becomes unreliable, fall back to storing full message history in chat_messages and replaying |
| D104 | M007/S01 | arch | `X-Conversation-Id` response header conveys new conversation ID to streaming client | The SSE Route Handler includes `X-Conversation-Id: <uuid>` in the response headers. The client reads this header before consuming the body stream to learn the created conversation ID on first turn. | SSE body is streaming — the client cannot wait for stream completion to learn the conversation ID (it needs it immediately to set up state for subsequent messages). Response headers are available before the body is consumed, making them the correct channel for this metadata. Avoids embedding the conversation ID in the SSE stream itself, which would require the client to parse for it mid-stream. | No |
| D105 | M007/S01/T01 | arch | D100 superseded: Agent SDK string prompt is correct; MCP registered via `mcpServers` option regardless | `ClaudeSDKClient.streamQuery()` uses `prompt: message` (string). D100 stated "async generator required for MCP". Verified against SDK v0.2.76 types: `prompt` is `string | AsyncIterable<SDKUserMessage>`. `SDKUserMessage` requires `{ type, message, parent_tool_use_id, session_id }` — not exposed for construction by SDK consumers. MCP tools are registered via `options.mcpServers` (independent of prompt form) and invoked by the model as needed. | String prompt is simpler, type-safe, and correct. The concern in D100 may have applied to a different SDK version or API. The diagnostic signal (model responds without real DB values) would surface if MCP tool registration failed for any other reason — not related to prompt form. `createSdkMcpServer` + `tool()` helper is the correct in-process MCP registration path; `McpSdkServerConfigWithInstance` is the type returned and passed in `mcpServers`. | No |
| D106 | M007/S01/T01 | arch | `createMonsterMcpServer()` returns `McpSdkServerConfigWithInstance`, not `McpServer` | The MCP server factory returns the result of `createSdkMcpServer({ name, tools })` — type `McpSdkServerConfigWithInstance` from the Agent SDK. The plan described returning `McpServer` (from `@modelcontextprotocol/sdk`). `ClaudeSDKClient.streamQuery()` accepts `McpSdkServerConfigWithInstance` in `opts.mcpServer` and passes it as `mcpServers: { monster: opts.mcpServer }` to `query()`. | `McpServer` from `@modelcontextprotocol/sdk` is the underlying instance, but the Agent SDK requires the full config wrapper (`McpSdkServerConfigWithInstance` = `{ type: 'sdk', name, instance: McpServer }`) in its `mcpServers` option. `createSdkMcpServer` constructs this wrapper. Passing a bare `McpServer` instance would cause a type error. | No |
| D107 | M007/S01/T01 | arch | Agent SDK streaming text via `SDKPartialAssistantMessage` (type `stream_event`) with `includePartialMessages: true` | `ClaudeSDKClient` sets `includePartialMessages: true` in query options. Text tokens arrive as `SDKPartialAssistantMessage` events: `{ type: 'stream_event', event: BetaRawMessageStreamEvent }`. Text is extracted when `event.type === 'content_block_delta' && event.delta.type === 'text_delta'`. Final `session_id` comes from `SDKResultMessage` (type `result`). | Without `includePartialMessages: true`, the SDK only emits `SDKAssistantMessage` (full message after completion) and `SDKResultMessage`. Text streaming requires partial messages. The `stream_event` wrapper carries the raw Anthropic API event — the same event type used in the direct Anthropic SDK streaming API. | No |
| D108 | M007/S01/T02 | impl | SSE ReadableStream controller close-safety: `closed` guard + try/catch in `send()` | `send()` function checks a `closed` boolean before enqueuing and catches `controller.enqueue()` errors to silently handle client-disconnect cases where the controller is already closed. The `finally` block checks the same flag before calling `controller.close()`. | Node.js ReadableStream controller throws synchronously if `enqueue()` is called after the controller closes (including client disconnect). Without the guard, normal client disconnects log spurious "Invalid state: Controller is already closed" errors. This is standard practice for SSE handlers. | No |
| D109 | M007/S01/T02 | arch | Pre-stream HTTP 404 for unknown `conversationId`, not SSE error event | When `conversationId` is provided but the conversation doesn't exist in DB, the Route Handler returns `HTTP 404 {"error":"Conversation not found"}` before opening the SSE stream. Slice plan implied SSE error event for bad conversationId. | Pre-stream HTTP error codes are correct for request validation failures that prevent the stream from starting. The client can check the HTTP status code without consuming the stream body. SSE error events (`data: {"type":"error","error":"..."}`) are reserved for runtime failures during streaming (e.g., SDK or MCP errors mid-stream). Returning 404 JSON before stream start is cleaner and more conventional for REST APIs. | Yes — if client framework requires all errors as SSE events |

| D110 | M007/S01/T03 | impl | URL update after first conversation turn uses `window.history.replaceState()` (not `router.push()`) | After the first SSE turn creates a new conversation, the client captures the `X-Conversation-Id` header and calls `window.history.replaceState(null, '', `/monster?c=${newConvId}`)` to update the URL without triggering a React/Next.js navigation. | `router.push()` would cause Next.js to re-render the server component page (fetching messages from DB, re-mounting ChatWindow), which would lose in-progress streaming state and cause a visible flicker. `replaceState` keeps the React component tree intact while making the URL bookmarkable for subsequent loads. On the next hard reload, `page.tsx` reads `searchParams.c` and pre-fetches messages normally. | Yes — if deep-linking behavior needs server-side rendering immediately after first turn |

| D111 | M007/S02/T01 | impl | DataForSEO Labs language_code: separate `LABS_LANGUAGE_CODE` map with 2-letter codes | Labs and SERP live endpoints use 2-letter codes (`'es'`, `'en'`, `'de'`, `'fr'`, `'it'`). `MARKET_CONFIG.language_code` holds 4-letter Merchant API codes (`'es_ES'`, `'en_US'`, etc.). Added `LABS_LANGUAGE_CODE: Record<string, string>` constant alongside `MARKET_CONFIG`. All three new methods (`keywordIdeas`, `serpCompetitors`, `googleSerpResults`) look up this map. | Labs endpoints return errors or empty results when passed 4-letter codes. Keeping the maps separate makes the distinction explicit and prevents future methods from accidentally using the wrong format. | No |

| D112 | M007/S02/T01 | impl | Migration applied via temp `pg` script (not Supabase CLI) | `supabase db push` failed because the remote migration history table was out of sync. Applied `ALTER TABLE research_sessions ADD COLUMN IF NOT EXISTS progress jsonb;` directly via a temporary `packages/db/apply-migration.mjs` script using the `pg` package (added and then removed as devDep). | The Supabase CLI's migration history tracking is not used in this project — all previous migrations were also applied directly. Using `pg` directly is consistent with the S01/T02 approach (described in T02-SUMMARY.md). Migration file still exists for documentation and future re-apply. | Yes — if migration tracking via CLI becomes necessary |

| D113 | M007/S02/T02 | impl | `NicheResearcherMcpServer` does NOT receive `supabase` param — both clients handle credential fetch internally | `createNicheResearcherMcpServer(dfsClient, spaceshipClient)` takes only the two API clients, not a `supabase` client. `DataForSEOClient` and `SpaceshipClient` both call `createServiceClient()` internally at credential-fetch time (D028 pattern). Passing `supabase` to the MCP server factory would add an unused parameter and break the clean separation of concerns. | `SpaceshipClient` constructor takes no arguments — it always creates its own Supabase client for credential lookup. `DataForSEOClient` does the same. The MCP server factory signature `(dfsClient, spaceshipClient)` is clean and matches the caller's perspective. | No |

| D114 | M007/S02/T02 | impl | `NicheResearcherJob` handler: `report` typed as `any` to satisfy Supabase `Json` assignability | `report` variable is declared as `eslint-disable any`. After `ResearchReportSchema.parse()`, the Zod inferred type is `ResearchReport`; after parse failure, it becomes `{ raw: string, error: string, parse_error: string }`. The Supabase update column type is `Json | undefined`, and neither union member satisfies `Json` without explicit casting. `any` is the pragmatic choice here — the data structure is validated by Zod before being stored. | TypeScript cannot narrow `unknown` → `Json` without a runtime type guard that mirrors the entire `Json` union (recursive). `any` at this boundary is justified: Zod validation ensures correctness before write; the alternative is a verbose and fragile runtime guard. | No |

| D115 | M007/S02/T03 | pattern | `export type {} from` re-export in `'use server'` files is safe for type-only exports (D034 extension) | D034 requires constants to live in `constants.ts`, not in `'use server'` files. However, types are erased at compile time and are not actual module exports at runtime. `export type { EnqueueResearchState } from './constants'` in `actions.ts` is safe because Next.js does not flag type-only exports in `'use server'` files — only value exports are restricted. | The `export type` keyword is a TypeScript construct that is fully erased during compilation. At runtime, the module has no additional exports. This means callers can `import type { EnqueueResearchState }` from the `actions.ts` entry point for convenience without violating the Next.js `'use server'` contract. | No |

| D116 | M007/S02/T03 | pattern | `enqueueResearch` uses insert-before-enqueue ordering with immediate failure recovery | The server action inserts the `research_sessions` row with `status='pending'` first, then calls `nicheResearchQueue().add(...)`. If enqueue fails after insert, the row is immediately updated to `status='failed'` with the error in `progress`. This prevents orphaned `pending` rows from accumulating if the BullMQ/Redis connection is down. | Orphaned pending rows would cause the UI to show a session stuck in `pending` state with no way to recover or retry. Immediate status update on enqueue failure ensures the session list shows an actionable state (failed) rather than a misleading one (pending forever). | No |

| D117 | M007/S03/T01 | pattern | `renderCompletedSession()` extracted as named async function in `page.tsx`; returns discriminated union | The completed-session branch in the research `page.tsx` is implemented as a named `async function renderCompletedSession(session)` rather than inline logic. Returns `{ type: 'ok', report, domains }` or `{ type: 'parse_error', raw, zodIssues }`. | Extracting to a named function keeps the main async server component body readable (a single switch/branch dispatch) while isolating the discriminated-union result type. The discriminated union makes TypeScript narrowing straightforward in the render path and makes the error branch impossible to accidentally ignore. | No |

| D118 | M007/S03/T01 | impl | Availability badges use inline `className` strings, not shadcn `Badge` `variant` prop | Domain availability badges in `ResearchReportViewer` use inline `className` strings (e.g. `className="inline-flex ... bg-green-100 text-green-800"`) rather than shadcn `Badge` variant. This is consistent with badge usage elsewhere in the codebase (`ResearchSessionStatus.tsx`, `page.tsx` BADGE constant). | shadcn `Badge` supports only `default / secondary / destructive / outline` variants — no green or yellow. Adding semantic color variants would require extending the shadcn component. Inline className is the correct lightweight approach when the component's built-in variants are insufficient and the codebase already follows this pattern. | No |

| D119 | M007/S03/T02 | impl | `defaultValue` (uncontrolled) used on niche Textarea and market NativeSelect, not `value` (controlled) | `SiteForm` is a `'use client'` component where `useActionState` owns submission state. Using controlled `value` would conflict with React Hook Form / `useActionState` patterns and require additional state management. `defaultValue` sets the initial rendered value without making the input controlled — the user can edit it freely before submitting. | Controlled inputs in a form that uses `useActionState` require explicit `onChange` handlers and state variables for each field. `defaultValue` on uncontrolled inputs is the correct pattern here: the pre-fill is a convenience default, not a value the form must keep synchronized with external state. | No |

| D120 | M007/S03/T02 | pattern | Next.js 15 async `searchParams` pattern: `searchParams: Promise<{...}>` + `await` in async server component | `/sites/new/page.tsx` declares `interface PageProps { searchParams: Promise<{ niche?: string; market?: string }> }`, makes the component `async`, and awaits `searchParams` before passing to child. Values arrive URL-decoded from Next.js — no manual `decodeURIComponent` needed. | Next.js 15 changed `searchParams` to return a Promise (breaking change from Next.js 14). Synchronous access (e.g. `props.searchParams.niche`) returns undefined without error, making this a silent failure if the old pattern is used. Awaiting is required. URL decoding is handled automatically by Next.js's URL parsing layer. | No |
| D121 | M008 | arch | Amazon CSV import is synchronous in Next.js server action (no BullMQ job) | `importAmazonCSV` is a `'use server'` action that processes the file synchronously in the request: `file.arrayBuffer()` → `TextDecoder` → `Papa.parse()` → Supabase upsert → return result. No BullMQ job or background processing. | Amazon Associates CSV reports are small (<100KB, typically <500 rows). Synchronous processing completes in <1 second — well within Next.js server action timeout. No BullMQ overhead, no job polling UI needed. Same pattern as other form actions in the project. | No |
| D122 | M008 | arch | Amazon CSV header normalization: dual-language lookup map (EN + ES); column-index fallback if no headers match | `AMAZON_HEADER_MAP` maps both English and Spanish column names to internal keys. If zero recognized headers are found after normalization, the parser throws with the list of unrecognized headers to aid debugging. No column-index fallback — the error message is sufficient to guide the user. | The column-index fallback adds complexity without clear benefit: if Amazon changes column positions, any fallback would silently produce garbage data. Failing visibly with the unrecognized header names is safer and more actionable. | Yes — add column-index fallback only if real Amazon CSV exports show headers that don't match either EN or ES map entries |
| D123 | M008 | arch | Unattributed CSV rows: returned in import result, never inserted into `revenue_amazon` | Rows whose `tracking_id` does not exactly match any `sites.affiliate_tag` are collected and returned as `unattributed: string[]` in the `ImportResult`. The UI displays them as a warning list. No DB row is inserted for unattributed tracking IDs. | `revenue_amazon.site_id` is NOT NULL — unattributed rows cannot be inserted without violating the constraint. Correct resolution: user updates the site's `affiliate_tag` to match the tracking ID, then re-imports the CSV (idempotent upsert). | No |
| D124 | M008 | arch | P&L computed in-memory in server component — no materialized view, no `revenue_daily` table | `computePnL()` is a pure function that reduces `costs` + `revenue_amazon` + `revenue_manual` rows from Supabase in-memory. `revenue_daily` table is not populated by CSV import and not used for P&L in Phase 1. | At Phase 1 scale (single user, <50 sites, <5K revenue rows), in-memory aggregation is instantaneous. No pre-aggregation needed. `revenue_daily` is designed for future API-sync use (R020, deferred). Adding a materialized view would add schema complexity with no user-visible benefit at this scale. | Yes — add aggregation when revenue row count exceeds ~50K or query latency becomes noticeable |
| D125 | M008 | arch | P&L CSV export is client-side Blob download — no API route needed | `PnLExportButton` constructs the CSV string in the browser from serialized `sitePnL` props, creates a `Blob`, and triggers a download via `URL.createObjectURL`. No `GET /api/finances/export` route. | The P&L data is already in the client component's props (passed from the server component). Building a server route would require re-fetching the same data. Client-side CSV construction is a one-liner for tabular data without nested structures. Date-range-specific filename (`pnl-{from}-{to}.csv`) can be computed from props. | Yes — add a server-side export route if the data volume grows beyond what comfortably serializes as props |
| D126 | M008 | arch | Domain expiry alerts sourced from `domains.expires_at`, filtered in-memory (60-day threshold) | Domain expiry alert computation: fetch all `domains` rows where `expires_at IS NOT NULL`, filter client-side (server-component in-memory) for those within 60 days of `new Date()`. No separate alerts table entry, no BullMQ job, no cron. | Domain count is always <200 in Phase 1 — in-memory filter is negligible. The P&L page already fetches `domains` for the sites join; adding `expires_at` to the select is free. A separate cron + `product_alerts` entry would add operational complexity with no advantage at this scale. The existing `product_alerts` table is for product-specific issues; domain expiry is a different concern. | Yes — move to a cron-based alert (inserting into `product_alerts`) if domain count exceeds 200 or if alerts need acknowledgement tracking |
| D127 | M008/S01 | arch | Amazon CSV `Artículos enviados` / `Shipped Items` column mapped but not stored | Real Amazon ES exports include a `Artículos enviados` (ES) / `Shipped Items` (EN) column not present in the original plan or DB schema. Added to `AMAZON_HEADER_MAP` as `items_shipped` so the parser normalizes it without throwing "Unrecognized CSV format". The value is discarded after normalization — `revenue_amazon` has no `items_shipped` column. | Skipping an unrecognized column would cause all rows to fail normalization if that column is the only unrecognized one and it appears in every row. The safe approach: normalize all known columns (even if unused), ignore values for keys not in the DB schema. This pattern should be applied to any future Amazon CSV column additions. | Yes — add `items_shipped` to DB schema if shipped item counts become analytically relevant |
| D128 | M008/S01 | arch | Supabase upsert `updated` count always 0 — no pre-query to distinguish insert vs update | `importAmazonCSV` reports `{ inserted: N, updated: 0 }` always. Supabase's `.upsert()` returns all affected rows without indicating which were new vs existing. A pre-query (count existing matching rows) would distinguish them but adds a round-trip and a race condition window. | At Phase 1 scale, the distinction between "new" and "re-imported" rows has no operational significance — the upsert is idempotent either way. The UI shows the combined count. If user needs to know exactly what was new, they can compare dates against the Revenue History table. | Yes — add pre-query count if the UX distinction between inserted and updated becomes a product requirement |
| D129 | M009 | arch | Amazon scraper in Node.js uses cheerio + rotating user agents; no Puppeteer/Playwright | `packages/agents/src/clients/amazon-scraper.ts` uses `fetch` with a rotating User-Agent pool (ported from tsa-monster's PHP pattern) + `cheerio` for HTML parsing. No headless browser. | VPS1 has limited RAM (CX22/CX32). Puppeteer/Playwright add ~300MB+ per browser instance and complicate deployment. Cheerio is a lightweight HTML parser with jQuery-like API — directly comparable to PHP's Symfony DomCrawler used in tsa-monster. If Amazon's anti-bot measures defeat fetch+cheerio, the next escalation is a proxy service, not a headless browser. | Yes — add proxy rotation if bare fetch gets blocked consistently |
| D130 | M009 | arch | Legal templates stored as markdown in DB; converted to HTML at Astro build time | `legal_templates.content` is a `text` column containing markdown. The Astro generator reads it from `site.json` and renders it via a markdown-to-HTML conversion at build time (using `marked` or Astro's built-in markdown support). Not HTML in DB. | Markdown is safe to edit in a plain textarea without XSS concerns. HTML in DB would require sanitization on edit and would bloat the settings form. Conversion at build time is zero-cost (already inside the static generation pipeline). Fallback to hardcoded default when no template assigned. | Yes — store as HTML if WYSIWYG editor is added later |
| D131 | M009 | arch | Chat sidebar toggle state stored in localStorage (`chat-sidebar-open`) | The sidebar open/closed state persists across navigation via `localStorage.getItem('chat-sidebar-open')`. No server state, no URL param, no React Context at layout level. | Server state for a UI toggle adds unnecessary complexity. URL param would pollute navigation history. localStorage is the minimal correct mechanism for "user preference that should persist across page loads in the same browser." Fallback: default closed if localStorage unavailable (SSR). | No |
| D132 | M009 | arch | System prompt DB overrides — agent_prompts table with agent_key + prompt_type | New `agent_prompts` table: `(id, agent_key text, prompt_type text, content text, updated_at)`. Unique constraint on `(agent_key, prompt_type)`. Agents call `getAgentPrompt(agentKey, promptType)` at job start; returns DB value if present, otherwise hardcoded default. | Storing prompts in DB enables runtime editing without code deploy. The hardcoded default ensures the system works without any DB rows (zero-config baseline). `agent_key` is a string enum in application code (e.g. `'content_generator'`, `'niche_researcher'`, `'monster'`). | No |
| D133 | M009 | arch | Search engine ping uses IndexNow API for both Google and Bing | After successful site deploy, `pingIndexNow(domain, urls[])` sends a single IndexNow request. Both Google (via indexnow.org) and Bing accept IndexNow submissions. One ping covers both engines. | IndexNow is the unified standard adopted by Bing, Yandex, and Google (via IndexNow.org). One API call covers multiple engines. Google Search Console API (OAuth2) would require credentials management for marginal benefit — IndexNow is simpler and sufficient. | No |
| D134 | M009 | arch | SEO files written post-build, directly to dist directory | After `astro build()` completes, `writeSeoFiles(distDir, siteData)` writes `sitemap.xml`, `robots.txt`, and `llm.txt` directly into the built dist directory. Not an Astro integration or plugin. | Post-build file writing is simpler than an Astro integration and avoids coupling to Astro's build lifecycle. The dist directory is fully formed after `build()` — writing additional static files into it is safe and idempotent. | No |
| D135 | M010/S01 | convention | setup-vps2.sh is idempotent — check-before-act for every mutating step | Tailscale install: `command -v tailscale` skip. Caddy install: `command -v caddy` skip. Caddyfile: `grep -q "import sites/\*"` skip. Sudoers: `grep -qF` skip. This means the script can be re-run safely on an already-configured VPS without side effects. | Idempotency is required for a bootstrap script — operators re-run it to verify state, apply updates, or recover from partial failures. Non-idempotent steps (duplicate apt sources, duplicate Caddyfile entries, duplicate sudoers lines) would be confusing and potentially break the server. | No |
| D136 | M010/S01 | arch | vps2-check.sh: --local mode + SSH mode in one script | `vps2-check.sh` supports three invocation forms: `--local` (no SSH, run checks directly on VPS2), `localhost`/`127.0.0.1` as host (same), or `<host> <user>` (SSH via agent). `run_check()` dispatches based on `LOCAL_MODE` flag. | The same check logic is needed both from VPS1 (remote SSH, verify post-setup) and from within setup-vps2.sh on VPS2 itself (local, no SSH needed). A single script with mode detection avoids duplication. | No |
| D137 | M010/S01 | arch | Caddy start failure treated as non-fatal warning in setup-vps2.sh | `systemctl start caddy` failure is caught with `|| true` and logged as `WARN`. Caddy may legitimately fail to start on a fresh VPS where `sites/*` glob matches nothing — this is expected behavior, not an error. | An empty `import sites/*` glob causes Caddy to log an error and refuse to start cleanly. The first actual site deploy (which writes a `.caddy` snippet to `/etc/caddy/sites/`) fixes this. Failing the entire bootstrap script over this would be wrong. | No |
| D138 | M010/S01 | convention | /bin/systemctl full path in sudoers entry | Sudoers line uses `/bin/systemctl reload caddy` not just `systemctl`. On Ubuntu 24.04, `/bin` is a symlink to `/usr/bin`, so this resolves correctly. Full path is required in sudoers — bare commands without path are ambiguous and may not match. | `visudo` accepts bare commands in some versions but it's not portable or safe practice. Full path is the standard sudoers convention and is explicitly required by `man sudoers`. Matches what CaddyService.ts calls (`sudo systemctl reload caddy` which the shell resolves to `/bin/systemctl`). | No |
| D139 | M010/S02/T01 | arch | InfraService reads settings internally (self-contained), unlike CaddyService/RsyncService which receive params | `getVps2Health()` and `testDeployConnection()` call `readVps2Settings()` which creates a `createServiceClient()` and reads `vps2_host`+`vps2_user` from the settings table. CaddyService and RsyncService take host/user as constructor/method params. | InfraService is called from server components and API routes — callers should not need to fetch settings themselves. CaddyService/RsyncService are called from BullMQ jobs that already batch-read all settings for multiple services. Different call sites, different ergonomics. `@monster/db` added as workspace dep to `@monster/deployment`. | Yes — if InfraService methods need to be called from contexts that already have settings, add overloads accepting host/user params |
| D140 | M010/S02/T02 | build | Webpack externals for node-ssh/ssh2 native modules | `next.config.ts` uses both `serverExternalPackages` and `webpack.externals` to exclude `node-ssh`, `ssh2`, and `cpu-features` from webpack bundling. `serverExternalPackages` alone is insufficient when the import originates from a workspace package (`@monster/deployment`). | `ssh2` includes native `.node` binaries (`cpu-features`, `sshcrypto`) that webpack cannot parse. `serverExternalPackages` applies to direct app imports but doesn't reliably externalize transitive deps from workspace packages resolved via `.pnpm`. Adding explicit `webpack.externals` in the `isServer` branch ensures these modules are always required at runtime. | Yes — may be resolved in future Next.js versions |
| D141 | M010/S03/T01 | convention | setup-vps1.sh mirrors setup-vps2.sh patterns (log/step helpers, arg parsing, banner format) | `setup-vps1.sh` uses identical `log()`, `step()`, banner formatting, and `--tailscale-key` argument parsing as `setup-vps2.sh`. | Consistency across VPS setup scripts makes both scripts predictable for the operator. Structured log lines (`[setup-vps1] [step N/6] [timestamp] LEVEL: message`) are parseable by agents and match the `[setup-vps2]` prefix convention. | No |
| D142 | M010/S03/T01 | convention | setup-vps1.sh runs as regular user (sudo only for Tailscale) | Unlike `setup-vps2.sh` which requires root, `setup-vps1.sh` runs as user `daniel`. Only Tailscale install/join commands use `sudo`. nvm, pnpm, pm2, git clone all run unprivileged. | VPS1 doesn't need system-level service configuration (no Caddy, no sudoers entries). Running as regular user is safer and matches how the operator actually provisions VPS1. | No |
| D143 | M010/S03/T02 | arch | deploy.sh pre-flight inlines SSH check instead of sourcing vps2-check.sh | Pre-flight logic is inlined directly in `deploy.sh` (~30 lines) rather than sourcing `scripts/lib/vps2-check.sh`. Three-branch check: `SKIP_VPS2_CHECK=1` → skip; `VPS2_HOST` empty → skip (backward compat); `VPS2_HOST` set → SSH + Caddy check. | Self-contained script is simpler to reason about. The inlined check uses `ssh -o BatchMode=yes` (same SSH agent pattern as RsyncService/CaddyService). `vps2-check.sh` is a more comprehensive diagnostic tool; deploy.sh only needs the pass/fail gate. | No |
| D144 | M010 | convention | SemVer version bump on milestone completion | Root `package.json` is the single source of truth for app version. `scripts/bump-version.sh <patch|minor|major>` bumps and commits. GSD agents run this at milestone completion. Criteria: patch = bug fixes, infra, polish; minor = new features, screens, integrations; major = breaking changes, rewrites. Version displayed in admin sidebar under brand. Initial version: 0.9.0. | Provides traceability between milestones and releases. Version visible in admin panel aids debugging/support. SemVer is the industry standard. | No |
| D145 | M011/S01/T02 | arch | HetznerClient reads hetzner_api_token from Supabase settings at call time (D028 pattern) — no constructor arg | `HetznerClient.getToken()` calls `createServiceClient().from('settings').select('value').eq('key','hetzner_api_token').single()` at the start of every API method. No constructor argument, no env var fallback, never logged. Consistent with `SpaceshipClient`, `InfraService`, `DataForSEOClient`. | Centralized credential storage in Supabase `settings` is the project's D028 pattern. All API clients follow it so operators can update tokens from the Settings UI without redeploying. | No |
| D146 | M011/S01/T02 | impl | registerSshKey handles 409 Conflict idempotently: POST → catch 409 → listSshKeys() → match by name | `registerSshKey(name, publicKey)` tries `POST /ssh_keys`. On `HetznerApiError` with `status===409`, calls `listSshKeys()` and finds the key by name, returning its ID. If name match fails, throws a named error. | Idempotency is required: Hetzner rejects duplicate key names but the operator key may already be registered from a prior provision. Silently returning the existing ID allows re-provisioning without manual Hetzner console cleanup. | Yes — if operator changes key with same name: the 409 path returns old key ID; add fingerprint match to detect this |
| D147 | M011/S01/T03 | impl | tailscaleKey passed directly into command string — never appears in any console.log | In `bootstrapVps()`: `conn.execCommand(\`bash /tmp/setup-vps2.sh --tailscale-key ${tailscaleKey}\`)`. No separate `console.log(tailscaleKey)` or `console.log('key:', opts.tailscaleKey)` anywhere in the file. | Tailscale auth keys are one-time-use secrets. If logged, they would be visible in pm2 logs and any log aggregation. Passing directly into the command string (without logging the full string) is the minimum viable approach. The plan constraint "never persisted or logged" is met. | No |
| D148 | M011/S01/T03 | impl | SSH connect retried 6×5s in bootstrapVps before throwing structured error | SSH is attempted up to 6 times with 5s delays. After the loop, `conn.isConnected()` determines success. Failure throws `[ProvisioningService] SSH connect failed after N attempts: <original error>`. | Hetzner reports `status=running` before sshd is fully ready — empirically 10-30s lag. A single attempt would fail even on healthy servers. 6×5s = 30s tolerance covers the observed lag. The original error is preserved in the thrown message for diagnostics. | Yes — increase retry count if SSH readiness lag > 30s is observed |
| D149 | M011/S02 | arch | `runDeployPhase()` removes all `vps2_*` settings reads; `cloudflare_api_token` pre-flight check also removed | `runDeployPhase()` in `deploy-site.ts` now queries the `servers` table for the first active server (ordered by `created_at` asc) instead of reading `vps2_host`, `vps2_user`, `vps2_sites_root`, `vps2_ip` from settings. The `cloudflare_api_token` pre-flight presence check is also removed — `CloudflareClient` reads its own token internally via D028 and throws `[CloudflareClient] cloudflare_api_token not configured` if absent. | Consistent with the D028 pattern: each service is responsible for its own credential lookup. The pre-flight check duplicated validation logic already in `CloudflareClient`. Removing it simplifies `runDeployPhase()` to a single DB lookup (servers table) rather than two (settings + servers). | No |
| D150 | M011/S02 | arch | `vps2SitesRoot` hardcoded to `/var/www/sites` in `RsyncService` after `servers` table migration | The `vps2SitesRoot` parameter is removed from `RsyncService.deploy()`. The remote sites root is hardcoded to `/var/www/sites`. The `servers` table has no `sites_root` column. | `CaddyService` always hardcodes `root * /var/www/sites/${slug}/dist` in the Caddyfile snippet. RsyncService must match. Adding `sites_root` to the `servers` table would be correct but unnecessary complexity — all Hetzner VPS servers provisioned via `setup-vps2.sh` use `/var/www/sites`. Hardcoding it is consistent and eliminates a settings-management burden. | Yes — add `sites_root` column to `servers` table if servers ever need different paths |
| D151 | M011/S02/T02 | arch | `getVps2Health()` removed entirely rather than deprecated; `FleetHealth`/`ServerHealth` replace `Vps2Health` in public API | `Vps2Health` removed from `packages/deployment/src/index.ts` exports. `getFleetHealth()` returns `FleetHealth { servers: ServerHealth[], fetchedAt: string }`. `ServerHealth` extends the old `Vps2Health` fields with `serverId` and `serverName`. No deprecated shim created. | T03 updates the only caller (`infra/page.tsx`) in the same slice — there is no window where the old method is needed. Keeping a deprecated `getVps2Health()` would require maintaining a backwards-compat DB query against the settings table that S02 is actively removing. Removing it completely is cleaner and enforced by the fact that typecheck would catch any remaining callers. | No |
| D152 | M011/S02/T03 | pattern | T01 settings cleanup applied in T03 (catch-up deviation) because T01 was marked `[x]` without execution | `constants.ts`, `actions.ts`, `settings-form.tsx` were updated in T03 when it was discovered that T01's `[x]` marker had no corresponding task summary and the files were unmodified. Applied all three T01 changes within T03's scope. Documented in KN006. | Task completion markers in slice plans are not reliable indicators of actual file state in multi-agent execution contexts. The closer/executor must verify file state directly, not trust the plan checkbox. This pattern is now documented in `KNOWLEDGE.md` as KN006. | No |
| D153 | M011/S03/T01 | pattern | `emit()` closure pattern in `ProvisioningService.provision()` — wraps optional `onProgress` callback; phases call `emit()` not `onProgress` directly | `emit()` is defined as a closure at the start of `provision()`: `const emit = (step, message) => { if (onProgress) onProgress(step, message); }`. All 5 phases call `emit()`. Null-safe: existing callers that pass no `onProgress` are unaffected — `emit()` silently no-ops. | Calling `onProgress?.()` inline at each phase would work too, but the closure avoids repeating the optional-chain on every call site and makes the "5 phases emit" pattern visually clear in code. The closure is also easier to test independently if needed. | No |
| D154 | M011/S03/T01 | pattern | SSE route follows D108 pattern verbatim: `closed` boolean guard + `send()` try/catch + `controller.close()` in `finally` | `POST /api/infra/provision` mirrors `monster/chat/route.ts`: `let closed = false`; `send()` function guards with `if (closed) return`; `finally { closed = true; controller.close(); }`. This prevents `controller.enqueue()` errors if the client disconnects before the provision completes. | The D108 pattern was established in M007/S01 for the Monster Chat SSE route and is the project-standard for any long-running SSE handler. Provision can take 3–5 minutes — client disconnect is a realistic failure mode. The `closed` guard ensures the Node.js stream terminates cleanly without crashing. | No |
| D155 | M011/S03/T02 | pattern | GET helper routes return hardcoded fallback 200 JSON — never throw to client when Hetzner token absent | `GET /api/infra/datacenters` and `GET /api/infra/server-types` wrap their `HetznerClient` calls in `try/catch`. On any error (including KN005 `hetzner_api_token not found`), they return `{ datacenters: FALLBACK_DATACENTERS }` or `{ serverTypes: FALLBACK_SERVER_TYPES }` with HTTP 200. | The provision form must always be usable — even before the operator has configured their Hetzner token. Returning an error from the GET routes would either break the form render or require the modal to handle a fetch failure on every load. The fallback lists (`['nbg1-dc3','fsn1-dc14','hel1-dc2']` and `['cx22','cx32']`) are the only values supported by the bootstrap setup-vps2.sh anyway. | Yes — add a `X-Fallback: true` response header if the form needs to show a "using defaults" warning |
| D156 | M011/S03/T02 | arch | `ProvisionSection` thin state owner + `ProvisionModal` pure prop-driven — RSC/client boundary isolation | `ProvisionSection.tsx` (`'use client'`) owns `open: boolean` state and renders either the button or `<ProvisionModal open={open} onClose={...} />`. `infra/page.tsx` (RSC) imports `ProvisionSection`, not `ProvisionModal`. `ProvisionModal` is a pure component driven entirely by props. | `infra/page.tsx` is a React Server Component. Importing a stateful client component directly into a RSC requires that component to not cross the "use client" boundary at the RSC boundary. The `ProvisionSection` wrapper lets `page.tsx` treat the entire provision feature as a single black box, while the internal `open` state lives correctly on the client. | No |
| D157 | M012 | data | `tsa_products.meta_description` added as persistent DB column (supersedes D058) | New migration adds `meta_description text` to `tsa_products`. `ProductForm` saves it; `updateProduct` action includes it. `ContentGenerator` already generates it — now it will be persisted to DB instead of held in-memory only. | D058 deferred persistence because no column existed. Now that the column is added, in-memory-only is no longer necessary and creates a UX gap (user can't see or edit the AI-generated meta). The revisability note on D058 explicitly anticipated this migration. | No |
| D158 | M012 | data | `sites.homepage_seo_text` new column for editable homepage SEO body text | New migration adds `homepage_seo_text text` to `sites`. Exposed in site edit form alongside existing `focus_keyword`. `SiteInfo` in `data.ts` gains this field; `generate-site.ts` SELECT includes it. Homepage template can render it as an SEO text block below the hero. | `sites.niche` is the niche description (internal, AI prompt context). `homepage_seo_text` is the user-visible SEO body text that appears on the rendered homepage — semantically distinct. Keeping them separate avoids overloading `niche` with a dual purpose. | No |
| D159 | M012 | convention | Site template slugs namespaced as `tsa/classic`, `tsa/modern`, `tsa/minimal` | Migration inserts new `site_templates` rows with `tsa/` prefix and updates all existing `sites.template_slug` values. Astro template switch logic updated to match. Old bare slugs remain in `site_templates` as dead rows (no sites reference them post-migration) to avoid FK breakage. | With only TSA in Phase 1 the bare names work but are ambiguous — `classic` for which site type? Namespacing now costs one migration and a find-replace across Astro page files. Doing it after adding a second site type would require migrating live site rows with user-generated content referencing the slugs. One migration now is strictly cheaper. | No |
| D160 | M012 | arch | Legal template placeholder substitution at Astro build time via `interpolateLegal(content, site)` | `interpolateLegal()` in `apps/generator/src/lib/legal.ts` replaces `{{site.name}}`, `{{site.domain}}`, `{{site.contact_email}}`, `{{site.affiliate_tag}}`, `{{current_year}}` using `String.prototype.replaceAll`. Called before `marked()` converts markdown to HTML. Placeholders are literal double-braces to avoid collisions with CSS/JS. | One template per language covers all sites of that language — no per-site legal content clutter. Substitution at build time (not runtime) keeps the output fully static. The alternative (per-site DB rows) would require duplicating and updating legal content for every new site. | No |
| D161 | M012 | arch | Settings page uses three shadcn `Tabs` (API Keys / AI Prompts / Deployment) — single page, no route split | Three logical groups in one `<Tabs>` component. No separate routes. State is client-only (tab selection not persisted to URL). | Settings is a single-user admin panel — deep-linking to a specific tab has no use case. One server component fetch loads all settings; tab switching is purely cosmetic. Adding routes would add three page files and three server fetches for no benefit. | No |
| D162 | M012 | arch | Agent system prompt `defaultValue` = DB override ?? hardcoded constant — user edits the full active prompt | `SettingsPage` exports hardcoded default prompt strings as constants and passes them to `SettingsForm` alongside the DB-fetched overrides. The textarea `defaultValue` = `agentPrompts[key] ?? DEFAULT_PROMPTS[key]`. Saving a non-empty textarea upserts to DB; saving empty deletes the DB row (restores hardcoded default). | The current behaviour (empty textarea when no override) makes the prompt editor useless — the user has no idea what they're editing over. Showing the active state (whether default or override) is the only ergonomic approach. The pattern is already used by the category AI generator (shows current seo_text, user edits, saves). | No |
| D163 | M012 | arch | Mobile hamburger nav uses `<script is:inline>` in each layout — no separate JS bundle | A small toggle function (`~8 lines`) is embedded directly in the layout's HTML via `<script is:inline>`. Toggles a `hidden` class on the mobile menu `<div>`. No import, no event bus, no build step. | Astro's static output model means any JS must either be `<script>` (processed by Vite, full module system) or `<script is:inline>` (verbatim HTML injection). A 8-line toggle doesn't warrant a Vite module. `is:inline` is zero-dependency, zero-build-cost, and produces the smallest possible HTML. | No |
| D164 | M012 | arch | `marked` added to `apps/generator` only; admin preview uses `await import('marked')` dynamic client-side import | Server-side: `marked` is a build-time dep of `apps/generator` — called synchronously during `astro build` in `[legal].astro`. Client-side preview in `TemplateForm`: `const { marked } = await import('marked')` on button click — no SSR, no bundle bloat, first preview load has ~50ms latency. | Adding `marked` to `apps/admin` would add it to the Next.js bundle. Dynamic import defers it until the user clicks Preview — zero cost for users who never preview. The generator is a Node.js build tool; synchronous require is fine there. | No |
| D165 | M012/S02 | arch | `pros_cons` JSONB round-trips as newline-joined text in textarea — serialize on save, deserialize on load | `formData.get('pros')` → split by `\n` → filter empty → `{pros: string[], cons: string[]}` on save. `(prosCons?.pros ?? []).join('\n')` for textarea `defaultValue` on load. | JSONB arrays need a human-editable text representation. Newline-separated is the most natural UX for list input. Split+filter handles blank lines and trailing newlines from user typing. Alternative (comma-separated) is ambiguous for multi-word items. | No |
| D166 | M012/S02 | arch | `product_all_content` SSE field uses collect-then-parse JSON strategy, not incremental streaming | The route buffers all Claude text chunks into `fullText`, strips code fences, parses JSON, then emits `{type:"field", name, text}` SSE events per field. | JSON fields cannot be streamed incrementally into separate textareas — you don't know which field you're in until the full object is parsed. The alternative (separate API calls per field) would require 5x Claude calls. One structured JSON call is faster and produces more coherent cross-field content. | No |
| D167 | M012/S02 | pattern | `fieldRefs` map (Record<string, RefObject<HTMLTextAreaElement>>) dispatches SSE field events to textarea refs without a switch statement | `const fieldRefs = { detailed_description: detailDescRef, pros: prosRef, ... }`. On `event.type === 'field'`, look up `fieldRefs[event.name]` and set `ref.current.value = event.text`. | A switch statement would require 5 cases and duplicate the field→ref mapping. The map pattern is O(1), self-documenting, and easy to extend with new fields. The route controls which field names are emitted — the map keys are the contract. | No |
